{
  "_comment": "Optimized RAG Configuration for Tlamatini",
  "_embedding_model_comment": "Switched from qwen3-embedding:8b (7.5 GB VRAM, 91% saturation on RTX 4070 Laptop) to nomic-embed-text:v1.5 (~270 MB VRAM). Frees ~7.2 GB VRAM, eliminates RAM<->VRAM swap thrash on embed calls, and is ~30x faster per chunk on this card. Run 'ollama pull nomic-embed-text:v1.5' once before first context-load. To revert, set back to qwen3-embedding:8b and bump chunk-size/k-* knobs back up.",
  "embeding-model": "Nomic-Embed-Text:latest",
  "chained-model": "glm-5.2:cloud",
  "access_aimed_prompt_model": "glm-5.2:cloud",
  "ollama_base_url": "http://127.0.0.1:11434",
  "ollama_token": "<ollama_token goes here>",
  "_section_django_port": "TCP port the bundled Django/Daphne web server binds (the chat UI + the chat WebSocket). Read by manage.py (_resolve_django_port) and applied to EVERY launch path by _apply_configured_port: the frozen double-click, the .flw file association, the frozen browser auto-open, source 'python manage.py runserver', and 'manage.py startserver'. An explicit [ipaddr:]port on the command line (e.g. 'runserver 9100') always WINS over this key. Move it off 8000 when Windows/Hyper-V has RESERVED that port and the app crashes with WinError 10013 ('an attempt was made to access a socket in a way forbidden by its access permissions'). Any free port 1-65535; default 8000; 9000 is a common safe choice. Fail-open: a missing / unreadable / out-of-range value falls back to 8000 and NEVER stops the server from starting. NOTE: a direct 'daphne'/'uvicorn' launch bypasses manage.py, so pass the port on that CLI yourself; and if you run the TeleTlamatini bridge, point its 'tlamatini.base_url' at the same port.",
  "django_port": 8000,
  "ANTHROPIC_API_KEY": "<ANTHROPIC_API_KEY goes here>",
  "_section_gemini": "Google Gemini API key. Surfaced at the top level so any future Gemini-direct tool (text/vision) can read it through config_loader.py without having to dig into acpx.agents. The same key is ALSO injected into the gemini ACP child via acpx.agents.gemini.env so that spawning the gemini CLI through acp_spawn JustWorks.",
  "GEMINI_API_KEY": "<GEMINI_API_KEY goes here>",
  "enable_unified_agent": true,
  "unified_agent_model": "glm-5.2:cloud",
  "unified_agent_base_url": "http://127.0.0.1:11434",
  "unified_agent_temperature": 0.0,
  "unified_agent_max_iterations": 4096,
  "unified_agent_llm_step_timeout_seconds": 180,
  "unified_agent_tool_output_char_cap": 24000,
  "_section_image_interpreter": "Image Interpreter Configuration - TRIPLE-MODEL pipeline: image_interpreter_model (interpreter 1, forensic OCR) + image_interpreter_model_2 (interpreter 2, holistic) run in PARALLEL, then image_merging_model fuses both. Seeded into the Image-Interpreter agent as interpreter_model_1/2 + merging_model defaults (Config -> Models dialog).",
  "image_interpreter_base_url": "http://127.0.0.1:11434",
  "image_interpreter_model": "qwen3.5:cloud",
  "image_interpreter_model_2": "gemma4:cloud",
  "image_merging_model": "glm-5.2:cloud",
  "image_interpreter_temperature": 0,
  "image_interpreter_image_path": "",
  "_section_mcp": "MCP System Configuration",
  "mcp_system_server_host": "127.0.0.1",
  "mcp_system_server_port": 8765,
  "mcp_system_client_uri": "ws://127.0.0.1:8765",
  "mcp_files_search_server_host": "localhost",
  "mcp_files_search_server_port": 50051,
  "mcp_files_search_server_max_workers": 10,
  "mcp_files_search_model": "glm-5.2:cloud",
  "mcp_files_search_client_uri": "ws://127.0.0.1:50051",
  "max_lines_search_files": 1024,
  "_section_internet": "Internet Search Configuration",
  "internet_classifier_model": "glm-5.2:cloud",
  "internet_classifier_verbose": true,
  "internet_classifier_max_iterations": 4,
  "internet_classifier_streaming": true,
  "internet_hint_words_mode": "extend",
  "internet_hint_words": [],
  "web_summarizer_model": "glm-5.2:cloud",
  "web_context_max_chars": 10000,
  "web_search_tool_name": "web_search",
  "web_search_tool_description": "A tool for searching the internet for current events, real-time information, or facts you do not know.",
  "_section_chunking": "Document Chunking - OPTIMIZED FOR CODE",
  "chunk_size": 2000,
  "_chunk_size_comment": "REDUCED to 2000 (was 3000) - Still captures full methods/classes; halves per-chunk embed latency vs 3000",
  "chunk_overlap": 300,
  "_chunk_overlap_comment": "REDUCED to 300 (was 800) - 2.7x less duplicated work per file with negligible recall loss",
  "max_chunks_per_file": 20,
  "_max_chunks_per_file_comment": "REDUCED to 20 (was 50) - Caps the chunk explosion on bloat files like boot.js (2.4 MB)",
  "_section_retrieval": "Retrieval Configuration - OPTIMIZED FOR RECALL",
  "use_mmr": false,
  "_use_mmr_comment": "DISABLED - Standard similarity is better for finding all relevant code pieces",
  "k_vector": 40,
  "_k_vector_comment": "REDUCED to 40 (was 100) - Still deep recall, 60% less re-ranking work",
  "fetch_k": 120,
  "_fetch_k_comment": "REDUCED to 120 (was 300) - Candidate pool 3x smaller, same precision after fusion",
  "mmr_lambda": 0.7,
  "enable_bm25": true,
  "_enable_bm25_comment": "ENABLED - Critical for keyword matching (function names, etc.)",
  "k_bm25": 40,
  "_k_bm25_comment": "REDUCED to 40 (was 100) - Match k_vector for balance",
  "rrf_k": 60,
  "k_fused": 60,
  "_k_fused_comment": "REDUCED to 60 (was 150) - Still saturates the LLM context budget",
  "_section_compression": "Context Compression - OPTIMIZED",
  "use_llm_extractor": true,
  "use_embeddings_filter": false,
  "embeddings_filter_threshold": 0.4,
  "use_long_context_reorder": true,
  "_section_context_limits": "Context Size Limits - OPTIMIZED",
  "max_doc_chars": 100000,
  "_max_doc_chars_comment": "REDUCED to 100k (was 250k) - boot.js (2.4 MB) and bundle.js (494 KB) get truncated cleanly instead of dominating the index",
  "max_context_chars": 200000,
  "_max_context_chars_comment": "REDUCED to 200k (was 450k) - Still fills a 128k-token context comfortably; less LLM-side rerank work",
  "_section_context_budget": "Context Budget Allocation",
  "context_budget_allocation": {
    "high_relevance": 0.6,
    "architecture": 0.2,
    "related": 0.15,
    "documentation": 0.05
  },
  "_context_budget_comment": "Prioritize high relevance (60%) and architecture (20%)",
  "_section_security": "Security",
  "redact_secrets_in_context": true,
  "_section_history": "Chat History Management",
  "history_summary_enable": true,
  "history_summary_trigger_tokens": 150,
  "history_keep_last_turns": 3,
  "_section_chat_agent_runtime": "Wrapped Chat-Agent Runtime Management",
  "chat_agent_limit_runs": 256,
  "_section_metadata": "Metadata Extraction",
  "metadata_extraction": {
    "enable_code_structure": true,
    "enable_file_role_classification": true,
    "enable_dependency_tracking": true,
    "enable_cross_references": true
  },
  "_section_retrieval_strategy": "Advanced Retrieval Strategy",
  "retrieval_strategy": {
    "enable_multi_stage": false,
    "_enable_multi_stage_comment": "DISABLED - Use standard RRF (Vector + BM25) for better code recall",
    "enable_query_expansion": true,
    "enable_hierarchical_context": true,
    "enable_context_budget_allocation": true
  },
  "_section_misc": "Miscellaneous Settings",
  "load_hidden": true,
  "binary_context_detection": true,
  "_binary_context_detection_comment": "Content-based binary detection for the RAG context loader (agent/rag/binary_guard.py). Drops binary files from the embedding chain even when their name/extension is not in the Context > Set file type omissions list. Every omission is logged with the --- [BINARY-GUARD] prefix in tlamatini.log. Set false to restore the pre-guard behaviour.",
  "binary_detection_sample_bytes": 8192,
  "binary_detection_control_ratio": 0.3,
  "binary_detection_log_each_file": true,
  "binary_detection_extra_binary_extensions": [],
  "binary_detection_force_text_extensions": [],
  "ssl_verify": true,
  "disable_proxies": false,
  "max_input_tokens": 10000,
  "keep_last_turns": 3,
  "ssl_cert_file": "",
  "requests_ca_bundle": "",
  "_section_performance": "Performance Tuning",
  "performance": {
    "enable_caching": true,
    "cache_embeddings": true,
    "parallel_processing": true,
    "max_workers": 6,
    "_max_workers_comment": "REDUCED to 6 (was 12) - 12 concurrent embed requests triggered thermal spikes on the PC GPU; 6 keeps the GPU saturated without forcing throttle"
  },
  "_section_logging": "Logging Configuration",
  "logging": {
    "log_retrieval_metrics": true,
    "log_context_size": true,
    "log_query_rewrites": true,
    "verbose_metadata": true
  },
  "_section_kali": "Kalier / MCP-Kali-Server bridge. 'kali_server_url' is the base URL of the Flask API (server.py) running on your Kali box — Tlamatini IS the client (the embedded replacement for Claude Desktop's client.py). The wrapped chat_agent_kalier tool auto-injects this as the default server_url on every run, so plain prompts ('scan 10.0.0.5 and give me a report') work without ever repeating the URL. Default http://127.0.0.1:5000 works when Kali runs in WSL2 with localhost forwarding (or you SSH-tunnel the port); otherwise set it to http://<KALI_LAN_IP>:5000 via Config -> URLs in the navbar. The LLM may still override it per-call by passing server_url=...",
  "kali_server_url": "http://127.0.0.1:5000",
  "_section_zavu": "Zavuerer / Zavu unified-messaging bridge (zavu.dev). 'zavu_api_key' is your Zavu REST API key - ONE key for SMS / WhatsApp / Telegram / Email / Voice. Set it once via Config -> Access Keys Wizard (the 'Unified Messaging (Zavu)' section) or here; the wrapped chat_agent_zavuerer tool auto-injects it on every run so chat prompts never repeat it. Get a key at https://www.zavu.dev (free sign-up; pay-as-you-go to send). Keep this EMPTY in any pushed/committed config - it is a secret.",
  "zavu_api_key": "<zavu_api_key goes here>",
  "_section_discoverer": "Discoverer / ProjectDiscovery recon suite. 'pdcp_api_key' is your ProjectDiscovery Cloud Platform key - OPTIONAL for every Discoverer tool (lifts cvemap/vulnx rate limits; enables nuclei -ai / cloud upload). Set it once via Config -> Access Keys Wizard (the 'Security Recon (ProjectDiscovery)' section) or here; the wrapped chat_agent_discoverer tool auto-injects it on every run so chat prompts never repeat it. Get a free key at https://cloud.projectdiscovery.io. Keep this EMPTY in any pushed/committed config - it is a secret.",
  "pdcp_api_key": "<pdcp_api_key goes here>",
  "_section_whatsapp": "Whatsapper / Meta WhatsApp Cloud API bridge (the official professional backbone). Get these from business.facebook.com -> WhatsApp Manager -> API Setup. 'whatsapp_phone_number_id' is the WABA sending number's ID (NOT the phone number). 'whatsapp_access_token' is a system-user PERMANENT access token (a secret - keep it out of source control; it is empty here and managed via data.keys / regen_secrets, or set it per-machine in Config -> URLs or the WHATSAPP_ACCESS_TOKEN env var). The wrapped chat_agent_whatsapper tool auto-injects all of these on every run so chat prompts ('send Ana a WhatsApp') never repeat them; the LLM may override any per-call. NOTE (WhatsApp policy): a free-form message only delivers inside the 24h window after the person last messaged you; to message someone COLD use an approved template.",
  "whatsapp_phone_number_id": "<whatsapp_phone_number_id goes here>",
  "whatsapp_access_token": "<whatsapp_access_token goes here>",
  "whatsapp_graph_base": "https://graph.facebook.com",
  "whatsapp_api_version": "v20.0",
  "_section_telegram": "Telegrammer / official Telegram bridge. 'telegram_bot_token' is the bot token from @BotFather (a secret - keep it out of source control; it is empty here and managed via data.keys / regen_secrets, or set it per-machine in Config -> URLs or the TELEGRAM_BOT_TOKEN env var). Telegrammer uses official Telegram surfaces only: Bot API by default, plus optional Telegram user-session credentials (telegram_api_id / telegram_api_hash / telegram_session_name or telegram_session_string) when private @username sends need a logged-in Telegram account. Human contacts/configs stay readable as @username; any Bot API numeric route belongs only in Telegrammer's private local cache. The wrapped chat_agent_telegrammer tool auto-injects this on every run so chat prompts ('send a Telegram to ...') never repeat it; the LLM may override it per-call by naming it.",
  "telegram_bot_token": "<telegram_bot_token goes here>",
  "_section_stm32": "STM32er / STM32 Template Project MCP bridge (https://github.com/XAIHT/STM32TemplateProjectMCP). ZERO-CONFIG by default: leave 'stm32_mcp_server_script' EMPTY and STM32er auto-bootstraps — it downloads the MCP from 'stm32_mcp_repo_url' into 'stm32_mcp_install_dir' (empty = a per-user LOCALAPPDATA/Tlamatini cache), pip-installs its deps (mcp, pyserial) and validates, then spawns and drives the stdio server. The user only installs STM32CubeIDE + Tlamatini. Set 'stm32_mcp_server_script' to an absolute mcp/stm32_mcp_server.py ONLY to point at an existing local checkout (an on-disk path wins and skips bootstrap). 'stm32_mcp_python' is the Python used to RUN the server + install its deps (empty = reuse Tlamatini's Python). 'stm32_template_dir' / 'stm32_ide_root' map to the server's STM32_TEMPLATE_DIR / STM32_IDE_ROOT env. The wrapped chat_agent_stm32er tool auto-injects all of these on every run so firmware prompts never repeat them; the LLM may override any per-call by naming it.",
  "stm32_mcp_server_script": "",
  "stm32_mcp_python": "",
  "stm32_template_dir": "",
  "stm32_ide_root": "",
  "stm32_mcp_repo_url": "https://github.com/XAIHT/STM32TemplateProjectMCP.git",
  "stm32_mcp_install_dir": "",
  "_section_esp32": "ESP32er / PlatformIO Core (pio CLI) bridge (https://platformio.org). ZERO-CONFIG by default: leave 'pio_executable' EMPTY and ESP32er auto-bootstraps — it downloads the official get-platformio.py installer (pip-install fallback) into 'pio_core_dir' (empty = a per-user LOCALAPPDATA/Tlamatini/platformio cache) on first use, so the user installs only the board USB driver + Tlamatini. Set 'pio_executable' to an absolute path of an existing pio/platformio binary ONLY to point at a pre-installed PlatformIO (it wins and skips bootstrap). The wrapped chat_agent_esp32er tool auto-injects both on every run so firmware prompts never repeat them; the LLM may override either per-call by naming it.",
  "pio_executable": " C:/Users/angel/.platformio/penv/Scripts/pio.exe",
  "pio_core_dir": "C:/Users/angel/.platformio",
  "_section_esphome": "ESPHomer / ESPHome (esphome CLI) bridge (https://esphome.io). ZERO-CONFIG by default: leave 'esphome_executable' EMPTY and ESPHomer auto-bootstraps — it `pip install esphome` into its Python on first use (ESPHome vendors PlatformIO + the toolchains it needs at first compile), so the user installs only the board USB driver + Tlamatini. Set 'esphome_executable' to an absolute path of an existing esphome binary ONLY to point at a pre-installed ESPHome (it wins and skips bootstrap). The wrapped chat_agent_esphomer tool auto-injects it on every run so device prompts never repeat it; the LLM may override it per-call by naming it.",
  "esphome_executable": "",
  "_section_arduino": "Arduiner / Arduino CLI (arduino-cli) bridge (https://arduino.github.io/arduino-cli/). ZERO-CONFIG by default: leave 'arduino_cli_executable' EMPTY and Arduiner auto-bootstraps — it downloads the official arduino-cli binary from downloads.arduino.cc into 'arduino_cli_install_dir' (empty = a per-user LOCALAPPDATA/Tlamatini/arduino-cli cache), runs config init + core update-index on first use, and AUTO-INSTALLS the board's core before a build, so the user installs only the board USB driver + Tlamatini. Set 'arduino_cli_executable' to an absolute path of an existing arduino-cli binary ONLY to point at a pre-installed CLI (it wins and skips bootstrap). The wrapped chat_agent_arduiner tool auto-injects both on every run so firmware prompts never repeat them; the LLM may override either per-call by naming it.",
  "arduino_cli_executable": "C:/Program Files/Arduino CLI/arduino-cli.exe",
  "arduino_cli_install_dir": "C:/Program Files/Arduino CLI",
  "allowed_paths": [
    "application",
    "docs",
    "downloads",
    "desktop",
    "pictures",
    "videos",
    "music"
  ],
  "_section_acpx": "ACPX Runtime + Skills (added 2026-04-29). The whole 'acpx' block is OPTIONAL — when missing, every value below is the safe default applied automatically by agent/acpx/config.py::load_acpx_config. Documenting the keys here makes the surface discoverable.",
  "_acpx_doc_url": "See ACPX.md at the repo root and docs/claude/acpx.md (when present) for the full contract.",
  "acpx": {
    "_acpx_cwd_comment": "Default working directory for ACP child processes when no per-spawn cwd is given. Empty string = use the Django process cwd at runtime.",
    "cwd": "",
    "_acpx_stateDir_comment": "Where AcpSession transcripts and per-session JSON records are written. Empty string = <app-base>/.tlamatini/acpx-state/ (kept under the installation directory to avoid writes to restricted user-profile folders on corporate machines).",
    "stateDir": "",
    "_acpx_probeAgent_comment": "agent_id used by acp_doctor's --version probe. Empty string = first resolvable agent in the registry.",
    "probeAgent": "",
    "_acpx_permissionMode_comment": "One of 'approve-reads' (default; reads auto-approved, writes need a prompt), 'deny-all' (lockdown; even acp_spawn is blocked), 'approve-all' (FLAGGED DANGEROUS; auto-approves every action including writes/shell/network). Mirrors OpenClaw's ACPX_PERMISSION_MODES vocabulary exactly.",
    "permissionMode": "approve-reads",
    "_acpx_nonInteractivePermissions_comment": "What to do when an action needs a prompt but no operator can answer (Multi-Turn unattended runs). 'deny' = action denied, run continues. 'fail' = whole spawn/skill fails hard.",
    "nonInteractivePermissions": "deny",
    "_acpx_timeoutSeconds_comment": "Per-turn timeout for the embedded runtime (acp_send / initial spawn drain). Default 120s; bump for slow CLIs like Gemini cold-start.",
    "timeoutSeconds": 120,
    "_acpx_pluginToolsMcpBridge_comment": "When true, inject a built-in MCP server into ACP children that exposes Tlamatini @tools to the child. OFF by default — opting a less-trusted runtime into our tool surface is a deliberate security choice.",
    "pluginToolsMcpBridge": false,
    "_acpx_openClawToolsMcpBridge_comment": "Reserved for forward compatibility with OpenClaw's openClaw-tools bridge. OFF by default.",
    "openClawToolsMcpBridge": false,
    "_acpx_strictWindowsCmdWrapper_comment": "Legacy compatibility field. Accepted and logged as ignored; the current windows_spawn module handles .cmd/.bat resolution itself.",
    "strictWindowsCmdWrapper": false,
    "_acpx_mcpServers_comment": "Optional named MCP servers to inject into ACP child sessions at spawn time. Empty by default. Each entry: { command: 'python', args: ['-m', '...'], env: {...} }.",
    "mcpServers": {},
    "_acpx_agents_comment": "Per-agent_id command overrides. Empty = use the built-in registry (claude/cursor/codex/copilot/gemini/qwen/pi/droid/iflow/kilocode/kimi/kiro/opencode/tlamatini). Override when the binary lives outside PATH. Each entry can carry an optional 'env' map injected at spawn time.",
    "agents": {
      "codex": {
        "command": "C:/Users/angel/AppData/Roaming/npm/codex.cmd",
        "env": {
          "OPENAI_API_KEY": "<OPENAI_API_KEY goes here>"
        }
      },
      "claude": {
        "env": {
          "ANTHROPIC_API_KEY": "<ANTHROPIC_API_KEY goes here>"
        },
        "command": "claude"
      },
      "gemini": {
        "env": {
          "GOOGLE_API_KEY": "<GOOGLE_API_KEY goes here>",
          "GEMINI_API_KEY": "<GEMINI_API_KEY goes here>"
        },
        "command": "gemini"
      }
    }
  },
  "_section_acpx_skills": "Skills are loaded from agent/skills_pkg/<dir>/SKILL.md. The catalog is self-discovering; nothing in config.json controls it. Run `python manage.py acpx_lint` to validate the catalog.",
  "_section_acpx_tools": "ACPX exposes 7 LangChain @tools to the unified agent (acp_spawn / acp_send / acp_kill / acp_doctor / list_acp_agents / invoke_skill / list_skills). Each is a row in the Tool table seeded by migration 0071_acpx_skills and is independently toggleable through the existing tools dialog UI.",
  "ollama_repeat_penalty": 1.9,
  "ollama_num_ctx": 63536
}
