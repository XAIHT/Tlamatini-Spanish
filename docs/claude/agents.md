<!--
═══════════════════════════════════════════════════════════════════
  ✦  T L A M A T I N I  ✦   —   "one who knows"
  Created by  Angela López Mendoza   ·   @angelahack1
  Developer · Architect · Creator of Tlamatini
  Tlamatini Author Banner — do not remove (Angela's name is kept in every build)
═══════════════════════════════════════════════════════════════════
-->
# Tlamatini — Agents (Creation, Catalog, FlowCreator, FlowHypervisor)

## Backend Agent Contract Registry (`agent/services/agent_contracts.py`)

Since the Flow Compiler / Contracts pass (May 2026), every connection-shape decision the canvas, the `.flw` loader, the Validate dialog, and the Start sequence make is funneled through a single backend registry: `get_agent_contracts()` in `agent/services/agent_contracts.py`. Each agent is described by an immutable `AgentContract` dataclass:

| Field | Meaning |
|---|---|
| `agent_type` | Canonical normalized id, e.g. `node_manager`, `gateway_relayer`, `kyber_keygen` |
| `display_name` | Human-readable name, e.g. `Node Manager`, `ACPXer`, `SSHer` (capitalization quirks centralized in `agent_paths.display_name_from_agent_type`) |
| `aliases` | Extra names that resolve to this agent type (gives "Node Manager" → `node_manager` for free) |
| `input_field_by_slot` / `output_field_by_slot` | Per-slot connection-field shape: which YAML key a connection on slot 0/1/2 of the canvas writes into. Forker/Asker put slot 2 into `target_agents_b`, Counter puts slot 2 into `target_agents_g`, AND/OR put slot 2 into `source_agent_2`, and so on |
| `connection_fields` | Every field name the registry treats as a connection (`source_agents`, `target_agents`, `output_agents`, `source_agent_1/2`, `target_agents_a/b/l/g`) — used to clear stale wiring before recompiling |
| `parametrizer_fields` | Tuple of fields a downstream Parametrizer can address. Centralizes what used to live in `views.PARAMETRIZER_SOURCE_OUTPUT_FIELDS` (e.g. `apirer` exposes `url`, `response_body`; `acpxer` exposes `agent_id`, `session_id`, `transport`, `settle`, `transcript_path`, `response_body`) |
| `secret_paths` | Dotted paths inside `config.yaml` that must be redacted from `.flw` exports (e.g. `tlamatini.password`) |
| `no_input` / `no_output` | True for endpoints (Starter has no input; FlowCreator/FlowHypervisor have neither) |
| `exclude_from_validation` | True for FlowCreator and FlowHypervisor (system agents that never appear in the validation list) |
| `singleton` | True when only one instance is allowed on the canvas (FlowCreator, FlowHypervisor) — affects pool naming (`flowcreator` instead of `flowcreator_1`) |
| `long_running` | True for FlowHypervisor / TeleTlamatini and infrastructure listeners — informs the watchdog and the Start sequence |
| `never_starts_targets` | True for Stopper / Cleaner — the canvas wiring still draws but the agent does not auto-launch its `target_agents` |
| `special` | Marker for Ender (`output_agents` = launch list, `target_agents` = kill list) and Parametrizer (artifact-driven mappings) |

`get_agent_contract(value)` resolves an agent name through the alias map and returns the contract; unknown names get a synthesized default contract. The registry is `lru_cache(maxsize=1)`'d so the disk-discovery + builtin-overrides pass runs exactly once per process.

The registry is exposed to the frontend via `GET /agent/agent_contracts/` (returns `list_contract_summaries()`), to the Validate button via `list_pool_agents_for_validation()` in `flow_compiler.py`, and to both `.flw` save/load and Start via `compile_flow_payload()`.

## Creating a New Agent (Step-by-Step)

**Full guide**: `Tlamatini/.agents/workflows/create_new_agent.md`

### Summary Checklist (8 Steps)

1. **Backend: Agent directory and script**
   - Create `agent/agents/<agent_name>/<agent_name>.py` + `config.yaml`
   - Copy boilerplate from `shoter.py` (PID management, reanimation, helpers)
   - `os.environ['FOR_DISABLE_CONSOLE_CTRL_HANDLER'] = '1'` MUST be first
   - Implement reanimation detection (`_IS_REANIMATED`)
   - If agent generates data for Parametrizer: emit `INI_SECTION_<TYPE><<<` blocks

2. **Backend: Django view for connection updates**
   - Add `update_<agent_name>_connection_view` in `views.py`
   - Register URL in `urls.py`

3. **Backend: Database migration**
   - Create `agent/migrations/<NNNN>_add_<agent_name>.py`
   - Seeds an `Agent` row with `agentDescription` as display name
   - Run `python Tlamatini/manage.py migrate`

4. **Frontend: CSS gradient**
   - Add 4-color gradient in `agentic_control_panel.css`
   - `.canvas-item.<css_class>` normal + hover rules
   - Gradient exists ONLY in CSS (sidebar inherits via `applyAgentToolIconStyle()`)

5. **Frontend: JavaScript (4 files)**
   - `acp-agent-connectors.js`: Add fetch connector function
   - `acp-canvas-core.js`: 6 locations (classMap, AGENTS_NEVER_START_OTHERS, removeConnection, removeConnectionsFor, mouseup handler)
   - `acp-canvas-undo.js`: Undo/redo handlers
   - `acp-file-io.js`: .flw load handlers

6. **Documentation: Update `agentic_skill.md`**
   - Add agent entry so FlowCreator AI can use it

7. **Documentation: Update `README.md`**
   - Agent count, project structure, classification, workflow table, glossary, changelog, API table

8. **Quality: Lint**
   - `python -m ruff check` (fix all)
   - `npm run lint` (fix errors only)

### Critical Naming Convention

**⚠️ CORRECTED 2026-07-26.** The DB `agentDescription` is **NOT** the source of truth — `agent/apps.py::AgentConfig.ready()` **deletes every Agent row on each startup** and rebuilds it from the `agents/` folder listing via `_canonical_agent_display_name()` → **`services/agent_paths.py::display_name_from_agent_type`**. That override map is where the name is really decided; a migration alone is overwritten on the next launch (this is what rendered PDFer as "Pdfer"). Two rules follow: **(a)** add every new agent to that map, and **(b)** if the agent's canvas connection handler in `acp-canvas-core.js` only tests a **hyphenated** literal (`video-analyzer`, `kyber-keygen`, `file-creator`, `monitor-log`, …), the display name MUST be hyphenated too — the JS lowercases without collapsing spaces, so a spaced name matches nothing and the connection is silently never saved. Change `chat_agent_registry.display_name` in the SAME pass (it keys the `agent_<display>_status` enable gate). Pinned by `agent/test_agent_display_names.py`; full story in `docs/claude/recent-fixes.md` (2026-07-26).

The display name transforms differently per context:

| Context | Transform | "Node Manager" | "Shoter" |
|---|---|---|---|
| CSS classMap key | `name.toLowerCase().replace(/\s+/g, '-')` | `'node-manager'` | `'shoter'` |
| Sidebar visual | Same as classMap via `getAgentTypeClass()` | `'node-manager'` | `'shoter'` |
| Connection handlers | `name.toLowerCase()` (preserves spaces) | `'node manager'` | `'shoter'` |

### Agent Lifecycle

- **Fresh start**: No `AGENT_REANIMATED` env var -> log truncated -> "STARTED"
- **Reanimation** (pause/resume): `AGENT_REANIMATED=1` -> log NOT truncated -> "REANIMATED" -> reanim files loaded
- PID file written on start, removed in `finally` block
- Concurrency guard: `wait_for_agents_to_stop(target_agents)` before starting downstream
- Ender clears all `reanim*` files on stop

### Connection Fields

- `target_agents: []` - agents to START after finishing (active agents)
- `source_agents: []` - agents whose logs to MONITOR
- `output_agents: []` - only for Stopper/Ender/Cleaner (canvas wiring, not starting)
- Special: OR/AND use `source_agent_1`, `source_agent_2`; Asker/Forker use `target_agents_a`, `target_agents_b`; Counter uses `target_agents_l`, `target_agents_g`

---

## All 87 Workflow Agent Types

> **Single source of truth for descriptions**: `agents_descriptions.md` at the repo root. The Django view `agent.views.agentic_control_panel` parses the `## Workflow Agents` tables in that file (with `README.md` as a legacy fallback for older deployments) and injects the `Description` column into the page as `agent_purpose_map` JSON. The frontend uses each entry as the **hover tooltip** over the sidebar agent and as the **canvas Description dialog body** on right-click. Editing a row's `Description` cell there changes both human docs AND the live UI text. `build.py` ships `agents_descriptions.md` next to the executable in frozen mode so the resolution works in both modes. The lookup is case- and punctuation-insensitive (`re.sub(r'[^a-z0-9]+', '', name.lower())`), so `Kyber-KeyGen`, `Kyber KeyGen`, and `kyberkeygen` all map to the same entry.

### Control Agents
- **Starter** - Entry point, launches first agents
- **Ender** - Terminates all agents, launches Cleaners. `target_agents` = agents to KILL, `output_agents` = agents to LAUNCH after, `source_agents` = graphical only
- **Stopper** - Kills specific agents based on log patterns
- **Cleaner** - Deletes logs/PIDs after Ender
- **Sleeper** - Waits N ms then starts next
- **Croner** - Scheduled trigger (HH:MM format)

### Routing Agents
- **Raiser** - Watches source log for pattern, starts downstream when found
- **Forker** - Auto-routes to Path A or B based on two patterns
- **Asker** - Interactive A/B choice for user
- **Counter** - Persistent counter, routes L (< threshold) or G (>= threshold)

### Logic Gates
- **OR** - Fires when EITHER of 2 sources completes (2 inputs, 1 output)
- **AND** - Fires when BOTH of 2 sources complete (2 inputs, 1 output)
- **Barrier** - Fires when ALL N sources complete (generalized AND)

### Action Agents
- **Executer** - Shell commands
- **Pythonxer** - Inline Python behind a STRICT correctness gate (`compile()` syntax floor + blocking Ruff when `ruff_blocking=true`, the default). **ALWAYS triggers `target_agents` afterwards — no matter what** (success, gate refusal, or runtime failure); the exit code drives only the LED and the Multi-Turn fix→re-ruff→retry loop, never whether downstream starts. (2026-05-29 — was "exit-code gates downstream"; see `docs/claude/recent-fixes.md`)
- **Prompter** - LLM prompt execution
- **Summarizer** - LLM text/log summarization
- **Crawler** - Web crawling with LLM analysis
- **Googler** - Google search + text extraction (Playwright)
- **Playwrighter** - Scripted interactive browser automation (Playwright). Drives a real browser through declarative steps (goto/click/fill/wait_for/extract/screenshot/assert/download) for authenticated/JS/multi-step flows that Crawler (static fetch) and Googler (search) cannot do. Deterministic; emits INI_SECTION_PLAYWRIGHTER; always triggers target_agents. Set `headless: false` to watch it drive and `hold_open_seconds: N` (alias `hold_open_ms`) to keep the browser visible N seconds AFTER the last step BEFORE it closes — this is the "wait before closing so I can see it" knob the LLM should pass on such requests. Canvas counterpart of the chat_agent_playwrighter Multi-Turn tool.
- **Apirer** - HTTP REST API calls
- **Gitter** - Git operations
- **Ssher** - SSH remote commands
- **Scper** - SCP file transfer
- **Dockerer** - Docker commands
- **Kuberneter** - kubectl commands
- **Pser** - PowerShell commands
- **Jenkinser** - Jenkins job triggers
- **Sqler** - SQL queries (external window)
- **Mongoxer** - MongoDB operations (external window)
- **Mover** - File move/copy with glob patterns
- **Deleter** - File deletion with glob patterns
- **Shoter** - Screenshot capture
- **Camcorder** - Physical-camera (webcam) capture via OpenCV (`cv2`). The hardware-camera sibling of Shoter (Shoter = screen, Camcorder = camera); **observational** like Shoter, but STILL captured in the Exec Report (2026-06-07 completeness contract: EVERY Multi-Turn agent appears). `capture_mode` ∈ `photo` (**DEFAULT** — one shot, `.jpg`) / `video` (a segment of `video_duration_seconds`, no audio, `.mp4` via `mp4v`); `camera_index` selects the device (0=default). **Resolution policy**: optional `resolution_width`/`resolution_height` default `0×0` = the camera's NATIVE mode (robust — webcams only support discrete modes, so forcing an unsupported one snaps to the nearest; the applied resolution is read back and logged); set `W×H` to REQUEST a mode (requested + read-back both logged). `warmup_seconds` lets the sensor auto-expose before the shot. Saves to the **Pictures known-folder** (`SHGetKnownFolderPath` FOLDERID_Pictures via ctypes, fallback `~/Pictures`) under `TlamatiniCamcorder` with a collision-proof `camcorder_<media>_<YYYYmmdd>_<HHMMSS>_<ms>_cam<idx>.<ext>` name (override the parent with `output_dir`). Emits an atomic `INI_SECTION_CAMCORDER` block (fields `output_path`, `output_dir`, `filename`, `media_type`, `camera_index`, `duration_seconds`, `resolution`, `fps`; body = `response_body`) for Parametrizer and ALWAYS triggers `target_agents` once the file is saved. Needs `opencv-python`. Visual canvas counterpart of the wrapped `chat_agent_camcorder` Multi-Turn tool.
- **Globber** - Read-only FILE discovery by glob/filename pattern under a directory (the Claude-Glob equivalent): returns matching file paths, newest-first by default. `pattern` (glob like `*.py` / `**/*.md`, `**` recursive), `path`, optional `sort_by` (`mtime`/`name`/`none`), `max_results` (default 500). Files only (not dirs). Read-only but still shown in the Exec Report per the all-Multi-Turn-agents rule. Emits an `INI_SECTION_GLOBBER` block (`pattern`, `path`, `matches`, `truncated`, `status`; body = file list) for Parametrizer and ALWAYS triggers `target_agents`. The enumeration step ahead of a Grepper / File-Interpreter / Editor; prefer it over an Executer dir/ls. Visual canvas counterpart of the wrapped `chat_agent_globber` Multi-Turn tool.
- **Grepper** - Read-only regex CONTENT search across a single file or a whole directory tree (the Claude-Grep equivalent): returns matching lines as `file:line:match`. `pattern` (Python regex), `path`, optional `glob` (basename filter like `*.py`), `case_insensitive`, `output_mode` (`content`/`files`/`count`), `max_results` (default 200). Prunes noise dirs (.git/node_modules/venv/__pycache__/dist/build) and skips binary/unreadable files. Read-only (never changes a file) but still shown in the Exec Report per the all-Multi-Turn-agents rule. Emits an `INI_SECTION_GREPPER` block (`pattern`, `path`, `glob`, `matches`, `files_searched`, `truncated`, `status`; body = matches) for Parametrizer and ALWAYS triggers `target_agents`. The discovery step ahead of an Editor / File-Interpreter; prefer it over an Executer findstr/grep. Visual canvas counterpart of the wrapped `chat_agent_grepper` Multi-Turn tool.
- **Editor** - Surgical in-place find-and-replace on a single existing text file (the Claude-Edit equivalent): replaces an EXACT `old_string` with `new_string`, by default requiring `old_string` to be UNIQUE (refuses on >1 match unless `replace_all: true`); `old_string_b64`/`new_string_b64` (base64) give a byte-exact channel for code with backslashes/quotes. Byte-exact (line endings preserved). **State-changing**, so it IS in the Exec Report. Emits an `INI_SECTION_EDITOR` block (`file_path`, `status` edited/not_found/not_unique/noop/error, `occurrences`, `replacements`; body = `response_body`) for Parametrizer and ALWAYS triggers `target_agents` so a downstream Forker can branch on `{status}`. Prefer it over File-Creator when only part of an existing file should change. Visual canvas counterpart of the wrapped `chat_agent_editor` Multi-Turn tool.
- **PDFer** - Tlamatini's DOCUMENT COMPOSER and the WRITE side of the document family: File-Extractor / File-Interpreter READ documents, **PDFer AUTHORS them**. Turns Tlamatini's own answer, Markdown, HTML, plain text, images and/or existing PDFs into ONE styled PDF. **ZERO new dependencies** — `markdown` + `xhtml2pdf` + `pymupdf` (fitz) + `reportlab` + `pillow` + `pypdf` already ship with Tlamatini and are already used by `agent/doc_generation`; the Markdown→HTML→PDF pipeline and `DEFAULT_CSS` are ported **INLINE** from `agent/doc_generation/mardown_to_pdf.py` (a pool subprocess cannot import `agent.*`), and every backend is imported LAZILY so a missing lib degrades to `status: engine_unavailable` instead of crashing at import. Runs ONE shape per run selected by `mode` ∈ `auto` (DEFAULT — sniffs the content: ≥2 HTML tags → `html`, images only → `images`, text+images → `mixed`, else `markdown`; this is what makes *"turn your last answer into a PDF"* a single call, since `prompt.pmt` Rule 6 makes Tlamatini emit HTML tables) / `markdown` (fenced code, REAL tables, optional `toc`) / `html` / `text` (escaped, monospace-safe) / `images` (`image_layout` ∈ `one-per-page` | `fit` — each page takes the image's own aspect — | `grid` with `grid_columns`) / `mixed` (cover + prose + captioned figures) / `merge` (append `input_pdfs`) / `info` (READ-ONLY: pages, bytes, metadata) / `validate` (probe the six backends, write nothing). Content comes from `input_text` or `input_file` (.md/.txt/.html); styling from `title`, `subtitle`, `author`, `page_size` (A4|Letter|Legal), `orientation`, `margins_mm`, `css`, `toc`, `page_numbers` (an xhtml2pdf footer frame rendering `página N de M` / `page N of M`, chosen by `document_language` ∈ `es` | `en`, which localizes ONLY PDFer's own chrome — footer + fallback title — never the user's content, and falls back to the build default on an unknown value). **OPTIONAL Ollama polish** (`ollama_polish`, default **false**) restructures the raw content into clean Markdown through an Ollama model over the stdlib `urllib` (the Talker/Whisperer/Reviewer pattern) BEFORE rendering — a failed polish NEVER loses the document (it falls back to the raw content and says so). Saves to the **Documents known-folder** (`SHGetKnownFolderPath` FOLDERID_Documents via ctypes, fallback `~/Documents`) under `TlamatiniPDF` with a collision-proof `pdfer_<YYYYmmdd>_<HHMMSS>_<ms>.pdf` name (`overwrite: false` by default appends `_2`/`_3` rather than clobber; `filename` is `basename`-sanitized so it can never escape `output_dir`); intermediate/downscaled images stage under `<app>/Temp/PDFer` per the Temp policy. **SAFETY PREFLIGHT** (fail-safe): REFUSES (`status: refused`) rather than write an empty or wrong document when the content is missing, the mode is unknown, a backend is absent, or `output_dir` is unwritable. Emits an `INI_SECTION_PDFER` block (fields `mode`, `source_type`, `output_path`, `output_dir`, `filename`, `page_count`, `bytes`, `images_used`, `engine`, `status`; body = `response_body`) and ALWAYS triggers `target_agents` — success, failure OR refusal — so a downstream Forker can branch on `{status}` / `{page_count}`. State-changing → captured in the Exec Report, and on the **Ask-Execs tier-A allowlist** (it can write to any `output_dir` + `filename`, exactly like File-Creator). The canonical unattended chain is `Starter → File-Interpreter → Parametrizer → PDFer → Ender`. Visual canvas counterpart of the wrapped `chat_agent_pdfer` Multi-Turn tool.
- **LaTeXer** - Tlamatini's LaTeX TYPESETTING agent and the typesetting sibling of PDFer: **PDFer COMPOSES** a PDF from Markdown/HTML/images, **LaTeXer TYPESETS** one from `.tex` source — real mathematics, bibliographies, numbered cross-references and an index. It embeds the **complete capability surface of the `mcp-latex-server` MCP NATIVELY, as an agent** (`create_latex_file` / `create_from_template` / `edit_latex_file` / `read_latex_file` / `list_latex_files` / `validate_latex` / `get_latex_structure` / `compile_latex`) — with **NO MCP server, no FastMCP/pydantic/uv, no stdio child to babysit and no catalog entry to activate** — and goes beyond it with whole-PROJECT compilation, a real convergence loop and readable log diagnostics. Direct-CLI sibling of Kalier/Nmapper/ESP32er (stdlib-only `agent/agents/latexer/latexer.py`: `subprocess` + `shutil` + `glob` + `re` + `urllib`; never imports `agent.*`), so it behaves identically in source and frozen builds. **⚠️ REQUIRES MiKTeX — the ONE prerequisite.** Tlamatini ships NO TeX distribution (a full one is several GB and the release must stay under 2 GB): the user installs **MiKTeX** once (https://miktex.org/download) and LaTeXer is functional forever after. MiKTeX is *strongly* preferred over TeX Live/MacTeX for one concrete reason — `--enable-installer` (exposed as `auto_install_packages`, default **true**) makes MiKTeX **download a missing `.sty` ON DEMAND, mid-compile**, so a document needing a package the user never installed still builds; TeX Live and MacTeX are detected and used but cannot self-heal. With no distribution at all it REFUSES cleanly (`status: refused`) naming MiKTeX, and `action: install` downloads + launches the OFFICIAL MiKTeX installer (the Nmapper "USE, not redistribution" model). Resolution order puts MiKTeX first and covers the very common per-user install (`%LOCALAPPDATA%\Programs\MiKTeX`). Runs ONE capability per run selected by `action` ∈ environment (`validate`, `install`) / authoring (`create_file`, `create_from_template` — templates `article` | `report` | `book` | `beamer` | `letter` | `cv` | `homework` | `spanish-article` —, `edit_file` with `edit_mode` ∈ replace|insert_before|insert_after|append|prepend, `read_file`, `list_files`, `validate_tex` — a STATIC brace/environment/reference lint that needs NO LaTeX installed —, `structure`) / build (`compile`, `compile_project`, `clean`, `scaffold_compile`). **`compile` resolves its source in priority order** — `tex_path` → `project_dir` (the master `.tex` is AUTO-DETECTED as the one holding both `\documentclass` and `\begin{document}`, so `\input` children are excluded automatically) → raw `input_text`; with `auto_preamble` (default true) a bare FRAGMENT like `$E=mc^2$` is wrapped in a generated preamble, which makes *"typeset this"* a single call. **The convergence loop is the heart**: pass 1 → `biber`/`bibtex` (chosen automatically from the source: `\addbibresource`/biblatex → biber, `\bibliography` → bibtex) → `makeindex` → `makeglossaries` → keep re-running while the log still says the references have not settled, up to `max_passes` (default 5). `use_latexmk: auto` prefers latexmk when available — **but latexmk is probed for USABILITY, not presence**: it ships with EVERY MiKTeX install yet is a **Perl script**, and most Windows machines have no Perl, so a naive `which()` would break the default build path; an unusable or empty-handed latexmk falls back to the built-in loop automatically and says so. `-interaction=nonstopmode` + `stdin=DEVNULL` are non-negotiable (they are what stop LaTeX hanging forever on an error). The `.log` is parsed into named errors with line numbers, missing PACKAGES (with MiKTeX guidance), warnings and box counts; when a build fails with no LaTeX-shaped error the RAW tool output is quoted, so "0 errors, no PDF" is never reported with nothing to act on. Builds run IN the source's own directory (so `\input`, `\graphicspath` and BibTeX resolve exactly as for any LaTeX user), then the PDF is delivered to the **Documents known-folder** under `TlamatiniLaTeX` with a collision-proof `latexer_<YYYYmmdd>_<HHMMSS>_<ms>.pdf` name (`filename` is `basename`-sanitized so it can never escape `output_dir`); scaffolded projects default to `<app>/Templates/LaTeXer` per the Templates policy. `keep_aux: false` tidies `.aux`/`.toc` after a SUCCESSFUL build and deliberately KEEPS them after a failure (that is how you debug); `clean` removes only auxiliary extensions — never a `.tex`, `.bib` or `.pdf`. **`shell_escape` is OFF by default and must stay off** — `\write18` lets a `.tex` file execute arbitrary commands. **SAFETY PREFLIGHT** (fail-safe): REFUSES rather than mis-typeset when the engine is absent, the source is missing, an `edit_file` anchor is not unique, or `output_dir` is unwritable. Emits an `INI_SECTION_LATEXER` block (fields `action`, `engine`, `distribution`, `tex_path`, `project_dir`, `output_path`, `output_dir`, `filename`, `page_count`, `bytes`, `passes`, `bibliography`, `errors`, `warnings`, `success`, `status`; body = `response_body`) and ALWAYS triggers `target_agents` — success, failure OR refusal — so a downstream Forker can branch on `{status}` / `{success}` / `{errors}`. **⚠️ THE REPAIR LADDER (v1.48.2, 2026-08-05/06) — "LaTeXer must never fail".** A `compile` no longer just succeeds or dies: with `repair` on (the default) a failed build escalates through **EIGHT rungs, strictly in `LADDER_RUNGS` order**, each firing only when the one before it produced no PDF — `1 lint` (static structural repair, before any compiler runs) → `2 preamble` (infer `\usepackage` from the commands actually used) → `3 rules` (deterministic rewrites of known-bad constructs) → `4 log_directed` (compile, read the real error, fix THAT line) → `5 acquire` (install a genuinely missing package) → `6 engine_swap` (retry under xelatex / lualatex — both take UTF-8 + system fonts natively) → `7 model` (ask an LLM, whose answer **re-enters the ladder at rung 1**) → `8 bisect` (quarantine the bad block and build the rest). **⚠️ Rungs 7 and 8 were REORDERED on 2026-08-05 and must NOT be swapped back**: deterministic-beats-probabilistic holds for rungs 1-6, but **bisect is the ONLY rung that DELETES THE AUTHOR'S CONTENT**, so ranking it before the model meant a document a model could have fully repaired instead shipped with a paragraph cut out — and because bisect almost always "succeeds" at producing *something*, the model rung became unreachable in practice. **Losing the user's work is the worst outcome available**, so the destructive rung is strictly last; the model's answer is still gated by lint + a truncation check + it must actually compile. **SAFETY CONTRACT (do NOT weaken):** every repair is applied to a COPY and re-linted, and one that makes the static lint *worse* is REVERTED and recorded as rejected (a fixer that can damage a document is worse than no fixer); the author's file on disk is never overwritten unless `repair_write_back: true` (the ladder builds from a sibling `*.latexer-fixed.tex`); every rung that fires is recorded in the `ladder` audit trace with what it changed and whether it helped; and a quarantined block is **reported by name and line number** — silently dropping content would make the agent a liar. The result dict gains `ladder` / `rungs_used` / `degraded` / `quarantined` / `build_path` / `engine_used`, and `repair_rungs` in config opts individual rungs out (`repair_model` must be configured or the model rung self-skips). Coverage: `agent/test_latexer_repair_ladder.py`. `status` ∈ compiled | compiled_with_errors (a PDF EXISTS but LaTeX reported errors — reported honestly, never called a success; a **DEGRADED** build that only produced a PDF because content was quarantined never claims clean success either) | created | edited | read | listed | validated | analyzed | cleaned | refused | not_found | not_unique | engine_unavailable | error. State-changing → captured in the Exec Report, and on the **Ask-Execs tier-A allowlist** (it writes `.tex` + PDF to free-form paths, edits in place, deletes aux files, and runs a real compiler). The canonical unattended chain is `Starter → Summarizer → Parametrizer → LaTeXer → Ender`. Visual canvas counterpart of the wrapped `chat_agent_latexer` Multi-Turn tool.
- **Recorder** - Microphone / audio-input capture via `sounddevice`, saved as a WAV (written with the stdlib `wave` module). The audio sibling of Camcorder (camera) and Shoter (screen): Shoter = screen, Camcorder = camera, Recorder = SOUND; **observational** like both, but STILL captured in the Exec Report (2026-06-07 completeness contract: EVERY Multi-Turn agent appears). By DEFAULT it records from the system's DEFAULT input device for `record_seconds` seconds; pick a specific mic on a multi-microphone machine with `device_index` (the agent logs the numbered input-device list at startup) or by a case-insensitive name substring with `device_name`. **Sample-rate policy**: `sample_rate` defaults to `0` = the selected device's NATIVE default rate (robust — the device always supports it; the rate actually used is read back and logged), or force one (`44100`/`48000`/`16000`). `channels` defaults to mono (`1`) and is clamped down to the device's reported max input channels so an over-request can never crash the capture. `input_gain_percent` applies a POST-capture digital gain (`100` = unity/default, `200` = +6 dB, `50` = −6 dB, `0` = silence) — it amplifies the noise floor and a hot signal CLIPS, so the count of samples that hit the int16 rail is logged and reported as `clipped_samples`. Saves to the **Music known-folder** (`~/Music`, localized e.g. "Música") under `TlamatiniRecords` with a collision-proof timestamped filename (override the parent with `output_dir`). Emits an atomic `INI_SECTION_RECORDER` block (fields `output_path`, `output_dir`, `filename`, `device_index`, `device_name`, `sample_rate`, `channels`, `duration_seconds`, `gain_percent`, `clipped_samples`, `format`; body = `response_body`) for Parametrizer and ALWAYS triggers `target_agents` once the file is saved. Needs `sounddevice`. Visual canvas counterpart of the wrapped `chat_agent_recorder` Multi-Turn tool.
- **Whisperer** - SPEECH-TO-TEXT (STT / voice recognition): turns SPOKEN AUDIO into a STRING of text — the speech-to-text sibling of Talker (text-to-speech). 100% SELF-SUFFICIENT for the microphone: it OPENS, CONFIGURES (channels clamped to the device max, sample rate, software gain) and RECORDS the mic ITSELF (`sounddevice` + `numpy`, downmix to mono + resample to 16 kHz) — it does NOT depend on the Recorder agent — or transcribes a given audio FILE (`input_source` mic/file/auto, `audio_file`). **Read-only/observational** (audio in → text out), but STILL captured in the Exec Report (2026-06-07 completeness contract: EVERY Multi-Turn agent appears). Transcription engine: **faster-whisper LOCALLY by default** (`engine: faster-whisper`) — it AUTO-DETECTS an NVIDIA GPU (`ctranslate2.get_cuda_device_count`) and **ALWAYS falls back to CPU (int8)** on a machine without one, AND auto-retries on CPU if the GPU path fails for any reason (driver/cuDNN/VRAM), so a PC without a usable GPU ALWAYS still gets a transcript; `model` ∈ tiny/base/small/medium/large-v3/large-v3-turbo (default `base`), `device` auto/cuda/cpu, `compute_type` auto (float16 on GPU, int8 on CPU). Cloud engines `cloud-groq` / `cloud-openai` (stdlib `urllib` multipart POST; key from config or `GROQ_API_KEY`/`OPENAI_API_KEY`; point `cloud_base_url` at an OWhisper server for a self-hosted "Ollama-style" STT endpoint). `language` "" auto-detects; `task: translate` outputs English; `vad_filter` skips silence. **NOTE — Ollama CANNOT transcribe** (it has no audio input), so recognition is always done by the ASR engine; `ollama_cleanup: true` only post-processes the FINISHED transcript's punctuation/casing with an Ollama LLM (failure there keeps the raw transcript). Saves the transcript `.txt` (and the captured WAV) under `Documents/TlamatiniTranscripts` (override with `output_dir`). Emits an atomic `INI_SECTION_WHISPERER` block (fields `transcript_path`, `audio_path`, `input_source`, `engine`, `model`, `device`, `language`, `duration_seconds`, `segments`, `word_count`, `status`; **body = the transcript text** = `response_body`) for Parametrizer and ALWAYS triggers `target_agents` (even on error, so a Forker can branch on `{status}` ∈ transcribed/empty/engine_unavailable/error). Needs `sounddevice`+`numpy` (mic) and `faster-whisper` (local engine; absent + no cloud key → `status: engine_unavailable`, NOT a crash). Visual canvas counterpart of the wrapped `chat_agent_whisperer` Multi-Turn tool.
- **AudioPlayer** - Audio-file PLAYBACK to a system OUTPUT device (speakers) via `soundfile` (decode) + `sounddevice` (stream). The playback counterpart of Recorder (mic-IN): Shoter = screen, Camcorder = camera, Recorder = mic-IN, AudioPlayer = speakers-OUT; **observational/output** (mutates no persistent state), but STILL captured in the Exec Report (2026-06-07 completeness contract: EVERY Multi-Turn agent appears). `audio_file` (REQUIRED) is the path to play (WAV/FLAC/OGG/AIFF, MP3 with a recent libsndfile). Plays to the system DEFAULT output by default; `device_index` / `device_name` pick another (the agent logs the numbered output-device list at startup). `volume_percent` is a SOFTWARE gain (`100` = unity, `200` = +6 dB, `0` = silence; clipped-sample count reported). **`time_played`**: `0` = the whole file once; `N>0` = exactly N seconds — a longer file is TRUNCATED, a shorter one is LOOPED (whole repeats + a final partial segment), done with a streaming wrap-around callback so a huge duration over a tiny file never allocates a giant buffer. **Sample-rate policy**: `sample_rate` defaults to `0` = the file's OWN native rate (correct pitch; read from the file, so the user need not supply it) — a non-zero value FORCES the output rate and alters pitch/tempo (not resampled). Saves nothing; it does NOT change the OS default audio endpoint (per-playback only). Emits an atomic `INI_SECTION_AUDIOPLAYER` block (fields `input_path`, `input_dir`, `filename`, `device_index`, `device_name`, `file_sample_rate`, `play_sample_rate`, `channels`, `volume_percent`, `clipped_samples`, `file_duration_seconds`, `time_played_requested`, `played_seconds`, `play_mode`, `loops`, `partial_segment`, `format`, `status`; body = `response_body`) for Parametrizer and ALWAYS triggers `target_agents` (error path emits `status: error`). Needs `sounddevice` + `soundfile`. Visual canvas counterpart of the wrapped `chat_agent_audioplayer` Multi-Turn tool.
- **VideoPlayer** - Video-file PLAYBACK (WITH audio) on a chosen DISPLAY (screen) via `ffpyplayer` (decode + synchronized audio + volume; its pip wheel BUNDLES ffmpeg + SDL — no external ffmpeg, no runtime download — collected into the frozen build by `build.py --collect-all ffpyplayer`) and OpenCV (`cv2`, already bundled) for the window. The on-screen sibling of AudioPlayer (speakers); **observational/output**, but STILL captured in the Exec Report (2026-06-07 completeness contract: EVERY Multi-Turn agent appears). If ffpyplayer is unavailable it degrades to **silent OpenCV-only** video (volume no-op, logged). `video_file` (REQUIRED) is the path (.mp4/.mov/.mkv/.avi/.webm, any ffmpeg container). `display_index` picks the monitor (`-1` = primary; enumerated via Win32 `EnumDisplayMonitors`, logged at startup). `volume_percent` is the audio level (`100` = full, capped at 100). **`time_played`**: `0` = the whole video once; `N>0` = exactly N seconds — a longer video is TRUNCATED, a shorter one is LOOPED (whole repeats + a final partial segment), via a wall-clock driver. `window_width`/`window_height` size the window (`0` = the video's native size; centered on the chosen display); `fullscreen` fills the monitor; `keep_aspect` (default true) letterboxes via cv2 `WINDOW_KEEPRATIO` instead of stretching. **Additional-params analysis**: OMITS playback-speed/fps (native timing), decode-resolution/codec (intrinsic; window size = display scaling only), and audio-track/subtitle selection (advanced). Emits an atomic `INI_SECTION_VIDEOPLAYER` block (fields `input_path`, `input_dir`, `filename`, `display_index`, `display_geometry`, `video_width`, `video_height`, `window_width`, `window_height`, `fullscreen`, `volume_percent`, `backend`, `has_audio`, `file_duration_seconds`, `time_played_requested`, `played_seconds`, `play_mode`, `loops`, `partial_segment`, `format`, `status`; body = `response_body`) for Parametrizer and ALWAYS triggers `target_agents` (error path emits `status: error`). Needs `ffpyplayer` + `opencv-python`. Visual canvas counterpart of the wrapped `chat_agent_videoplayer` Multi-Turn tool.
- **Talker** - TEXT-TO-SPEECH (TTS): speaks `input_text` aloud through a system OUTPUT device (speakers) by driving an **OLLAMA** connection that runs a neural TTS model (default `Orpheus-3b-FT`). The voice-synthesis sibling of the media family (AudioPlayer plays an existing FILE; Talker GENERATES speech from text); **observational/output**, but STILL captured in the Exec Report (2026-06-07 completeness contract: EVERY Multi-Turn agent appears). Builds an Orpheus prompt (`<voice>: <text>`), streams the model's audio TOKENS over the Ollama HTTP API (stdlib `urllib`), decodes them to a 24 kHz mono waveform with the **SNAC** neural codec, saves a WAV, and plays it. **FEMALE-ONLY by design (Tlamatini is female; a male voice is FORBIDDEN BY DESIGN, no override).** `voice` ∈ the permitted FEMALE voices only: `tara` [default], `leah`, `jess`, `mia`, `zoe`; `gender` accepts only `female` (a shortcut that picks `tara` when `voice` is empty). A male voice (`leo`/`dan`/`zac`) or `gender: male` is REFUSED fatally — `talker.py::resolve_voice` raises `MaleVoiceForbiddenError` and `main()` closes the process entirely (`os._exit`) reporting "⛔ MALE VOICE IS FORBIDDEN BY DESIGN … NOW CLOSING.. BYE"; it never substitutes a female voice and never sounds male. `emotion` weaves a paralinguistic tag (`laugh`/`chuckle`/`sigh`/`cough`/`sniffle`/`groan`/`yawn`/`gasp`) into the speech (or inline `<laugh>` in `input_text`); `language` is a hint (base model is English-only, multilingual fine-tunes accept others; woven into the prompt when `include_language_in_prompt`). Connection: `ollama_url`, `ollama_token`, `model`. Generation: `temperature`, `top_p`, `top_k`, `min_p`, `repetition_penalty` (keep ≥ 1.1), `max_tokens`, `seed`, `request_timeout`. Playback/output: `play_audio`, `device_index`, `device_name`, `volume_percent`, `sample_rate` (0 = native 24 kHz), `output_dir` (default `Music/TlamatiniTalker`). Emits an atomic `INI_SECTION_TALKER` block (fields `output_path`, `output_dir`, `filename`, `model`, `language`, `voice`, `gender`, `emotion`, `sample_rate`, `audio_seconds`, `char_count`, `played`, `status`; body = `response_body`) for Parametrizer and ALWAYS triggers `target_agents` (`status: spoken`/`saved`/`tokens_only`/`error`). Fetching tokens needs only the stdlib; **rendering audible audio needs `snac` + `torch`** (a neural vocoder) — without them Talker saves the tokens and reports `status: tokens_only` (graceful degradation, NOT auto-bundled into the build). Visual canvas counterpart of the wrapped `chat_agent_talker` Multi-Turn tool.
- **Mouser** - Mouse/keyboard simulation (PyAutoGUI)
- **Keyboarder** - Keyboard typing / hotkey automation
- **Windower** - Window manager (Win32/pywin32, self-contained, ports the window-management subset of Microsoft's Windows-MCP incl. the AttachThreadInput focus dance). Acts on the WINDOW itself (peer of Mouser=clicks-inside, Keyboarder=types-into): `action` ∈ `list` / `focus` / `minimize` / `maximize` / `restore` / `move` / `resize` / `move_resize` / `close` / `topmost` / `untopmost` / `arrange` (snap/tile). Matches `window_title` by `substring`/`exact`/`regex` (+ `match_index`); emits `INI_SECTION_WINDOWER` (`action`, `window_title`, `matched`, `match_count`, `state`, `left`, `top`, `width`, `height`); always triggers `target_agents`. State-changing → in the Exec Report. Canvas counterpart of the wrapped `chat_agent_windower` Multi-Turn tool.
- **File-Creator** - Creates files with specified content
- **File-Interpreter** - LLM reads and interprets file contents
- **File-Extractor** - Raw text extraction (PDF, DOCX, etc.)
- **Image-Interpreter** - TRIPLE-MODEL LLM vision analysis: `interpreter_model_1` (default `qwen3.5:cloud`, forensic OCR/measurement) and `interpreter_model_2` (default `gemma4:cloud`, holistic context/people) run in PARALLEL, each on its OWN dedicated Ollama connection; a BARRIER waits until BOTH interpretations arrive, then `merging_model` (default `glm-5.2:cloud`) fuses them into ONE definitive report. All four prompts (`prompt_user` + `prompt_interpreter_model_1/2` + `prompt_merging_model`) get the image FILE NAME injected (`{filename}` placeholder or auto-appended) as an identity clue. Emits `INI_SECTION_IMAGE_INTERPRETER` (`file_path`, `interpreter_model_1/2`, `merging_model`, `status`; body = merged report). Fail-safe: one failed interpreter still merges from the survivor; a failed merger falls back to the raw interpretations. `llm:` keeps only shared `host`/`token`; a legacy `llm.prompt` (old .flw) is honored as the user prompt
- **J-Decompiler** - JAR/WAR decompilation (bundled jd-cli)
- **De-Compresser** - Deterministic short-running archive worker (compress OR decompress). Direction inferred from extensions; supports `.gz` / `.zip` / `.7z` / `.tar.gz` / `.gz.tar`; password from `DE_COMPRESSER_PWD` env var when `passwordless=false`. Always triggers `target_agents` at end-stage, even on failure.
- **Telegrammer** - Sends or receives Telegram messages using official Telegram surfaces only; people stay as `@username` in configs and Bot API routing ids stay private cache state. `provider` picks the sending IDENTITY: `bot` (Bot API, as the bot) or `user` (your own logged-in account, for private `@username` sends); plain words "as me"→`user` / "as the bot"→`bot`; `auto` (default) uses your account for private `@usernames`/`+phone`, the bot for numeric ids/channels. Sending as `user` needs a one-time Telegram login (a headed login window pops once).
- **TeleTlamatini** - Long-running Telegram bot that bridges authorized users into the full Multi-Turn + Exec Report + **ACPX** Tlamatini chat. **Fully authorized / ungated by design**: the per-tool "Ask Execs" gate is a browser modal a Telegram user can't answer, so the bot hard-pins `ask_execs_enabled: false` on every request and runs every state-changing tool unattended (the access password is the authorization). Run it on a **dedicated** Tlamatini account — the room group, request global-state, and the Ask-Execs broker are all keyed by user id, so sharing the account with a browser human cross-talks
- **Whatsapper** - Short-lived WhatsApp send/receive agent. `provider` picks WHICH NUMBER sends: `cloud` (default) = Meta's official WhatsApp Cloud API from the BUSINESS number (cold sends require an approved template; free-form text only works inside Meta's 24-hour customer-service window); `web` = sends from YOUR OWN personal number by automating WhatsApp Web (Playwright persistent profile) after a one-time QR login — no templates, no System User, but UNOFFICIAL (Meta-ban risk). Plain words: "as me"/"from my own WhatsApp"→`web`, "official"/"as the business"→`cloud`. The `whatsapp.web` block (`headless`/`login_wait_seconds`/`settle_seconds`/`profile_dir`) tunes web mode; web ignores `template`.
- **Instant Messaging Doctor** - Diagnoses and (optionally) safely repairs **Telegrammer / Whatsapper** messaging readiness using the official Telegram Bot API and the Meta WhatsApp Cloud API. Validates bot tokens, Meta access tokens, contacts-book entries, recipient reachability, WhatsApp templates + Meta's 24-hour customer-service window, and webhook config; **non-mutating by default** (`retry_send: false`), only attempting an approved-template or reachable-Telegram resend when explicitly asked. `mode` ∈ `auto` / `diagnose` / `repair` / `test`; `platform` ∈ `both` / `telegram` / `whatsapp`. Emits an `INI_SECTION_INSTANT_MESSAGING_DOCTOR` block for Parametrizer/Forker flows and ALWAYS triggers `target_agents`. The automatic failure companion (Telegrammer/Whatsapper failures launch it with the same contact/message context) and a pre-flight for mission-critical sends. Visual canvas counterpart of the wrapped `chat_agent_instant_messaging_doctor` Multi-Turn tool.
- **ACPXer** - Visual ACPX session driver: invokes one external coding-agent CLI (Claude/Codex/Gemini/Cursor/Qwen/etc.) per turn — for `oneshot-prompt` agents (claude/cursor/gemini/qwen/codex) the prompt is passed as a CLI arg and stdout is captured to EOF; for `tui-repl`/`json-acp` agents stdin/stdout is drained by the transport-aware idle rule. Captures last-assistant text, kills child. Canvas-driven counterpart of the 12 LLM-facing `acp_*` tools; emits Parametrizer-compatible `INI_SECTION_ACPXER` so multi-CLI relay flows can be drawn visually
- **Blenderer** - Drives a Blender instance via the OFFICIAL Blender MCP add-on's TCP socket (default `localhost:9876`; https://www.blender.org/lab/mcp-server/). Unlike Unreal's verb-dispatch protocol, the Blender MCP wire format is a **code-execution** protocol: each run sends `{"type":"execute","code":<python>,"strict_json":<bool>}` (NUL-framed) and Blender runs that Python (which must assign a `result` dict), replying `{"status":"ok"|"error","result":...,"message":...,"stdout":...}`. So Blenderer exposes a **rich action catalog** via `command` instead of forcing hand-written Python: PASSTHROUGH `execute_code` (raw `bpy`); READ-ONLY `ping`/`scene_info`/`get_objects`/`get_object_detail`/`blendfile_summary`; MUTATING/OUTPUT `create_object` (cube/sphere/cylinder/cone/plane/monkey/torus)/`delete_object`/`set_material`/`screenshot`/`render` (file outputs default under `<app>/Temp`). Self-contained (stdlib NUL-byte socket, no `mcp`/`blmcp` dep in the pool) — Tlamatini speaks the add-on socket DIRECTLY, skipping blender.org's `blmcp` bridge and bare chat client, so the whole Tlamatini canvas / Multi-Turn / Exec Report / Parametrizer stack sits on top. Captures the response into an `INI_SECTION_BLENDERER` block and ALWAYS triggers `target_agents` on success or failure. Requires Blender running with the add-on enabled, "Online access" on, and the server started; the agent does not launch Blender. Visual canvas counterpart of the wrapped `chat_agent_blenderer` Multi-Turn tool.
- **Unrealer** - Drives an Unreal Engine 5 editor via the Unreal MCP plugin's TCP socket (default `127.0.0.1:55557`). Each run sends ONE JSON command (`{"type": <verb>, "params": {...}}`) and forwards whatever the connected plugin build exposes — the extended surface is 53 commands across nine categories: editor (actors + `focus_viewport`/`take_screenshot`), blueprint (authoring + `set_pawn_properties`), node (graph wiring + `find_blueprint_nodes`), project (input mappings), umg (widgets), **system** (`execute_python`/`execute_console_command`/`get_class_info`/`list_assets`), **level** (open/save/new/get_current_level), **asset** (import/duplicate/rename/delete/save/create_folder), **material** (create/instance/set_parameter/assign). The P3 headless tools (build_project/run_automation_tests/run_macro) are NOT bridge commands — they shell out to UnrealEditor-Cmd and are unreachable over this socket; chain Unrealer nodes via Parametrizer for the run_macro equivalent. Captures the engine response verbatim into an `INI_SECTION_UNREALER` block (so a downstream Forker / Raiser can branch on `status` / `error`) and ALWAYS triggers `target_agents` on success or failure. Visual canvas counterpart of the wrapped `chat_agent_unrealer` Multi-Turn tool. The recommended plugin is Tlamatini's own extended Unreal MCP fork (the Unreal Engine MCP modified specifically for this system) at `https://github.com/XAIHT/XaihtUnrealEngineMCP.git` — a drop-in built on upstream `chongdashu/unreal-mcp` that ships the full 53-command surface. That fork ALSO ships a deterministic **project scaffolder** (`scaffold_unreal_project.py`): from just a project name + directory it stands up a brand-new **UE 5.8** C++ project with the UnrealMCP plugin pre-wired, EngineAssociation pinned to 5.8 (auto-discovering the installed UE 5.8 even when unregistered), and a **Visual Studio 2026** solution generated — driven from the Catalog-of-Prompts entry `prompt-106` (two `[[ ]]` markers = project name + directory). The template carries the UE-5.8/VS-2026 build fixes: `Target.cs` `BuildSettingsVersion.V7` + `IncludeOrderVersion.Unreal5_8`; a project-root `Directory.Build.targets` that keeps VS 2026's `SetEnv` under the 32,767-char `INCLUDE` limit; `FImageUtils::PNGCompressImageArray`; and a **pre-fixed VisualStudioTools plugin** (`BlueprintGraphClasses.h`→`K2Node_CallFunction.h`) bundled so VS 2026 doesn't inject a broken one. Agent param fixes (2026-07-12): `assign_material` maps `slot`→`slot_index` (the plugin's wire key) and `material_path` is `/Content`→`/Game` normalized. Full contract: `docs/claude/recent-fixes.md` (2026-07-12).
- **Reviewer** - LLM-powered code reviewer. Resolves a `git diff` for the configured `repo_path` (`diff_ref` like `HEAD~1` / `origin/main`, or empty = uncommitted working-tree + staged changes), sends it to an Ollama model with a senior-engineer review prompt, parses a `verdict` (`APPROVE` / `REQUEST_CHANGES` / `COMMENT`), and emits an `INI_SECTION_REVIEWER` block (fields `repo_path`, `diff_ref`, `verdict`, `model`, `status`; body = review). ALWAYS triggers `target_agents` so a downstream Forker can branch on `{verdict}`. Visual canvas counterpart of the `code-review` SKILL.md.
- **Analyzer** - Deterministic static-analysis / security scanner (no LLM). Runs whichever of `bandit`, `semgrep`, `ruff`, `eslint`, `gitleaks`, `pip-audit` are on PATH over `target_path`, aggregates findings, and emits an `INI_SECTION_ANALYZER` block (fields `target_path`, `tools_run`, `tools_skipped`, `total_findings`, `status` ∈ `clean`/`findings`/`error`; body = combined output). ALWAYS triggers `target_agents` so a downstream Forker can gate on `{status}` / `{total_findings}`. Visual canvas counterpart of the `security-audit` SKILL.md.
- **MCP Doctor** - STATIC, on-paper triage of the External MCP catalog (the `external_mcps.json` shape — see `docs/claude/architecture.md` → *External MCPs*) — the offline counterpart of the live `external_mcp_doctor` LLM tool. **WITHOUT connecting** it reads the catalog and reports, per server: `transport` (stdio / streamable-http / sse / websocket / tcp / named-pipe), `runtime` (docker / npx / uvx / python / node / bun / deno / java / .NET / …), a command-on-PATH probe, placeholder-secret detection, blockers, and a `next_step`. Self-contained (stdlib-only `agent/agents/mcp_doctor/mcp_doctor.py`; does NOT import `agent.*`, like the other pool agents). `server_key` triages ONE server, empty = all; `catalog_path` / `source_url` override where the catalog is read from. Emits an `INI_SECTION_MCP_DOCTOR` block (fields `server_key`, `transport`, `url`, `runtime`, `supported`, `status`, `catalog_path`; body = `response_body`) for Parametrizer and ALWAYS triggers `target_agents` so a downstream Forker can branch on `{supported}` / `{status}`. Visual canvas counterpart of the wrapped `chat_agent_mcp_doctor` Multi-Turn tool (and the canvas counterpart of the `external_mcp_doctor` LLM tool — `chat_agent_mcp_doctor` is the static read; `external_mcp_doctor` is the live connect-and-probe).
- **Kalier** - Bridge to **Kali Linux** offensive-security tooling via the MCP-Kali-Server (`https://www.kali.org/tools/mcp-kali-server/`). POSTs to the MCP-Kali-Server Flask API (`server.py`; default `http://127.0.0.1:5000`) over the stdlib `urllib` (no `requests`/`mcp` deps in the pool) and runs ONE capability per run selected by `action` ∈ `command` / `nmap` / `gobuster` / `dirb` / `nikto` / `sqlmap` / `metasploit` / `hydra` / `john` / `wpscan` / `enum4linux` / `health`. Captures the tool's stdout/stderr into an `INI_SECTION_KALIER` block (fields `action`, `endpoint`, `method`, `subject`, `return_code`, `success`, `timed_out`, `server_url`; body = output) and ALWAYS triggers `target_agents` so a downstream Forker can branch on `{success}` / `{return_code}`. For a remote Kali box use an SSH tunnel (or a LAN IP). **Embedded-client UX**: the wrapped `chat_agent_kalier` auto-injects the global `kali_server_url` (set once in **Config ▸ URLs** / `config.json`, default `http://127.0.0.1:5000`) as the default `server_url`, so chat prompts never repeat the Kali box address — the LLM may still pass `server_url=` to override for a one-off box; canvas/.flw runs set `server_url` in the node dialog. Authorized targets only. Visual canvas counterpart of the wrapped `chat_agent_kalier` Multi-Turn tool.
- **Discoverer** - Bridge to the **ProjectDiscovery** security-tool suite (`https://github.com/projectdiscovery`) for recon / attack-surface mapping / vulnerability discovery. Runs ONE tool per run selected by `tool` ∈ `subfinder` (passive subdomain enum) / `httpx` (HTTP probe + fingerprint) / `naabu` (port scan) / `katana` (crawler) / `nuclei` (template vuln scan) / `cvemap` (CVE search — the `cvemap` tool now RUNS ProjectDiscovery's `vulnx` binary, since cvemap's own CVE API was retired Aug 2025; drives `vulnx search`/`id` with filters `cvemap_severity`/`cvemap_product`/`cvemap_limit`), plus meta `bootstrap` / `validate` / `update_templates` / `list_tools`. Direct-CLI sibling of Kalier/ESP32er/Arduiner (stdlib-only `agent/agents/discoverer/discoverer.py`; no MCP server, never imports `agent.*`). **ZERO-CONFIG PRIVATE GO TOOLCHAIN**: on the first call it downloads the official Go compiler from `https://go.dev/dl/` into `<install_dir>/Go` (GOROOT) and `go install`s the requested tool into `<install_dir>/Go/bin-tools` (GOBIN) — no system Go, no PATH mutation (slow once, then cached). Tool resolution order: explicit → private GOBIN → `~/.pdtm/go/bin` → PATH. The `pdcp_api_key` (PDCP_API_KEY) is OPTIONAL for ALL tools (lifts cvemap's rate limit, enables nuclei `-ai`/cloud); subfinder reads its own provider-config.yaml (`subfinder_provider_config`). Set the PDCP key once in **Config ▸ Access Keys Wizard ▸ 'Security Recon (ProjectDiscovery)'** (or `config.json` `pdcp_api_key` / the `PDCP_API_KEY` env / `data.keys`) — `tools._seed_global_agent_defaults` **auto-injects** it into every `chat_agent_discoverer` run, `agent_contracts` **redacts** it from `.flw` exports, and `regen_secrets.py` toggles it push-able↔keyed. **SAFETY PREFLIGHT** (fail-safe): requires a target for the scanning tools, downgrades naabu SYN-scan on Windows to a warning (defaults to a CONNECT scan — no Npcap) and katana-headless-without-Chrome to a warning, and REFUSES (never crashes) when correctness can't be guaranteed. Emits an `INI_SECTION_DISCOVERER` block (fields `tool`, `target`, `returncode`, `success`, `findings_count`, `json_path`, `pdcp_used`, `stage`; body = output) and ALWAYS triggers `target_agents` so a downstream Forker can branch on `{success}` / `{findings_count}`. **AUTHORIZED TARGETS ONLY** (subfinder/cvemap passive; httpx/naabu/katana/nuclei active). Visual canvas counterpart of the wrapped `chat_agent_discoverer` Multi-Turn tool.
- **Zavuerer** - Bridge to **Zavu** (`https://www.zavu.dev`) — ONE unified messaging API for SMS / WhatsApp / Telegram / Email / Voice. Sends through Zavu's single REST endpoint (`POST /v1/messages`) with one API key instead of separately wiring Twilio + Meta's WhatsApp Cloud API + SMTP; `channel: auto` lets Zavu's ML pick the best channel with automatic fallback (e.g. WhatsApp fails → SMS). Runs ONE capability per run selected by `action` ∈ `send` (post a message — `to` a +E.164 phone for SMS/WhatsApp/Voice/Telegram or an email for Email, `text`, `channel`, optional `subject`/`from_sender`/`fallback`) / `health` (probe the API + whether the key is configured). Direct-HTTP sibling of Kalier/Apirer (stdlib-only `urllib` `agent/agents/zavuerer/zavuerer.py`; no SDK, never imports `agent.*`). The `zavu_api_key` (get it at zavu.dev — free sign-up, but Zavu charges pay-as-you-go per message sent) is set once; an empty key makes a `send` REFUSE with `status: refused` (routable, not a crash). **SAFETY PREFLIGHT** (fail-safe): refuses rather than mis-send when the key / recipient / text is missing or the channel is unknown. Emits an `INI_SECTION_ZAVUERER` block (fields `action`, `channel`, `to`, `status`, `message_id`, `success`, `base_url`; body = `response_body`) and ALWAYS triggers `target_agents` so a downstream Forker can branch on `{success}` / `{status}`. **AUTHORIZED, OPTED-IN RECIPIENTS ONLY** (A2P / the WhatsApp 24-hour window / GDPR). Visual canvas counterpart of the wrapped `chat_agent_zavuerer` Multi-Turn tool.
- **Video-Analyzer** - The "eye" of the **Robotic-Loop-Training** scheme and the MOTION-VERDICT sibling of Image-Interpreter: it WATCHES A RECORDED VIDEO and rules whether a physical system (e.g. an STM32-driven servo) performed the requested motion. Pipeline: resolve the video (`video_pathfilenames` = a file / wildcard / folder-newest / a **Camcorder pool name** whose last `output_path` is read) → extract `num_frames` frames with OpenCV (in memory) → DETERMINISTIC motion gate (peak inter-frame diff in an optional `roi`; below `motion_threshold` → `FAIL_NO_MOTION` with NO model call) → if motion is present, `interpreter_model_1` (default `qwen3-vl:235b-cloud`, the video/temporal specialist) and `interpreter_model_2` (default `qwen3.5:cloud`) judge the timestamped frames IN PARALLEL on two dedicated Ollama connections → BARRIER → `merging_model` (default `glm-5.2:cloud`, text-only) fuses them and issues the FINAL verdict with a **safety override**: `PASS_OK` is accepted ONLY when BOTH interpreters independently agree (else `UNCLEAR` — a false PASS is the worst outcome). Emits an atomic `INI_SECTION_VIDEO_ANALYZER` block (`video_path`, `verdict`, `verdict_token`, `confidence`, `motion_score`, `frames_analyzed`, the three model names, `status`; body = merged report) AND a dedicated **SUBSTRING-SAFE** `TLM_VERDICT::<TOKEN>` line so a downstream Forker branches cleanly (pattern_a `TLM_VERDICT::PASS_OK` → finish, pattern_b `TLM_VERDICT::FAIL` → reprogram; `PASS_OK` is never a substring of any `FAIL` token, so a failure can never route to success). ALWAYS triggers `target_agents` (even on error). Self-contained (stdlib + `cv2` + `numpy`, already bundled; never imports `agent.*`). Read-only/observational (mutates no state) but captured in the Exec Report like every Multi-Turn agent. Needs `opencv-python`. Visual canvas counterpart of the wrapped `chat_agent_video_analyzer` Multi-Turn tool.
- **Nmapper** - Tlamatini's LOCAL, **USE-ONLY** nmap bridge for pentesters / CTF recon and the local, zero-install sibling of Kalier (remote Kali box) and Discoverer (ProjectDiscovery suite). It runs a real `nmap` the user installed themselves and **NEVER bundles or redistributes nmap** (nmap's NPSL forbids embedding it in a redistributed product without a paid OEM licence), resolving an installed nmap in order: explicit `nmap_executable` → PATH → `C:\Program Files (x86)\Nmap\nmap.exe` / `C:\Program Files\Nmap\nmap.exe` → a `%LOCALAPPDATA%\Tlamatini\nmap` copy. If none resolves it REFUSES gracefully (fail-safe preflight) with one-time install guidance, and `action=install` (or `auto_install`) downloads + launches the OFFICIAL FREE nmap self-installer from nmap.org — the user's OWN download + admin install (which also installs Npcap, unlocking SYN / `-O` / raw UDP). The **DEFAULT is an UNPRIVILEGED TCP CONNECT SCAN** (`-sT`) that needs NO Npcap and NO admin, so a fresh install scans immediately; raw-packet features degrade gracefully on Windows without Npcap — a requested SYN scan auto-downgrades to `-sT` and `-O` is dropped (both warned), and a `udp` action without Npcap is refused (no connect-scan fallback) — never a crash. Runs ONE capability per run selected by `action` ∈ `quick` (`-sT -sV -sC -Pn -T4 --top-ports 1000`, the CTF opener) / `full` (`-p-`, all 65535 TCP ports) / `top_ports` / `version` / `scripts` (targeted NSE via `nse_scripts`) / `host_discovery` (`-sn`) / `udp` / `custom` (safe base + `custom_args`, which are validated to reject shell metacharacters) / `validate` (report nmap + Npcap resolution) / `install`. Knobs: `target` / `targets_file`, `ports`, `top_ports`, `timing` (T0..T5, default T4), `scan_technique` (connect | syn), `version_detect`, `default_scripts`, `os_detect`, `skip_host_discovery` (`-Pn`, default true), `min_rate`, `output_dir`. It runs with `-oX` (parsed for `hosts_up` / `open_ports`) + `-oN` (the human report = body) and emits an `INI_SECTION_NMAPPER` block (fields `action`, `target`, `scan_technique`, `ports`, `return_code`, `success`, `hosts_up`, `open_ports`, `npcap_present`, `xml_path`, `output_path`, `stage`; body = `response_body`) so a downstream Forker can branch on `{success}` / `{open_ports}`, and ALWAYS triggers `target_agents` (even on a fail-safe refusal). Self-contained (stdlib only: subprocess + shutil + urllib + xml.etree; never imports `agent.*`). State-changing → captured in the Exec Report. **AUTHORIZED TARGETS ONLY** (e.g. scanme.nmap.org); treat scan output as untrusted data. Visual canvas counterpart of the wrapped `chat_agent_nmapper` Multi-Turn tool.
- **STM32er** - Bridge to **STM32 firmware** development across the **WHOLE ST 32-bit line** — from the **Blue Pill (STM32F103)** up through F7 / G0 / G4 / L0..L5 / H7 / U5 / WB — via **two backends** selected by `stm32_backend` (default `auto`): (1) the **PlatformIO backend** (Phase 1) drives PlatformIO Core's `ststm32` platform DIRECTLY (the ESP32er pattern — no MCP server — over stdlib-only `_pio_*` helpers in `agent/agents/stm32er/stm32er.py`); pick a `board` (e.g. `bluepill_f103c8`, `nucleo_h743zi`, `disco_f407vg`), pio knows every board's memory map/linker, and it **zero-config auto-installs PlatformIO Core into the SHARED per-user core dir** (`%LOCALAPPDATA%/Tlamatini/platformio` — the SAME install ESP32er uses). (2) The **template-MCP backend** (STM32F407VG + rich serial/SWD HIL) drives the STM32 Template Project MCP (`https://github.com/XAIHT/STM32TemplateProjectMCP`) — a FastMCP stdio server over a self-contained inline MCP stdio JSON-RPC client (no `mcp` dep in the pool). **Routing (Phase 0):** under `auto` a `board`, a non-STM32F4 `device`, or a PlatformIO-only action (`list_boards`/`scaffold_build_flash`/…) routes to PlatformIO; blank/F407 stays on the template MCP (today's exact flow, zero regression). `_device_family` now recognises every ST family; the shared `_emit_and_trigger` tail emits ONE `INI_SECTION_STM32ER` for both backends. Runs ONE capability per run selected by `action`. **PlatformIO actions:** `list_boards` / `create_project`(needs `board`) / `write_source` / `read_source` / `list_sources` / `build` / `clean` / `flash`(=upload) / `build_and_flash` / `list_artifacts` / `monitor` / `monitor_session` / `pkg_install` / `pkg_list` / `check` / `test` + the one-call composite `scaffold_build_flash` (create→write→build→flash-over-ST-LINK→optional monitor); `framework` defaults to `arduino` (stm32duino — a board-agnostic blink works everywhere), or `stm32cube`/`cmsis`/`mbed`/`libopencm3`. **Template-MCP** surface is **27 actions** = the **23 MCP tools** (firmware scaffold / build / flash / observe — incl. serial + SWD) + **2 composites** (`serial_session`, `live_monitor`) + **2 meta** (`bootstrap`, `validate`). **ZERO-CONFIG AUTO-BOOTSTRAP** (`_bootstrap_mcp`): with no on-disk `server_script` (the default `stm32_mcp_server_script` is now empty) and `auto_bootstrap: true`, STM32er downloads the MCP itself — shallow `git clone` with a GitHub-zip fallback when git is absent — into `%LOCALAPPDATA%/Tlamatini/STM32TemplateProjectMCP`, pip-installs `mcp` + `pyserial` if missing (`pip_install: true`), and validates it, so a user installs **only STM32CubeIDE + Tlamatini**. **SAFETY PREFLIGHT** (`_preflight`, fail-safe): before any compile/flash it validates compiler / CubeIDE / make / programmer / ST-LINK driver + probe (`_probe_stlink`) / device family (`_device_family`) and **REFUSES** rather than mis-build or mis-flash — compile-only actions need no board, hardware actions (flash / erase / reset / `serial_*` / SWD / `live_*`) require a connected ST-LINK, and on the template-MCP backend a cross-family target is refused **and ROUTED to the PlatformIO backend** (the MCP template is STM32F407VG-specific). The PlatformIO backend has its OWN fail-safe preflight (`_pio_preflight`): it routes each family, WARNS on the partially-supported ones (L5/U5/WL), and REFUSES only the newest silicon (STM32C0/H5/U0/WBA/N6) — which awaits the ST-native **STM32CubeCLT backend (Phase 2/3, not yet implemented; STM32N6 also needs external-flash boot + a signed FSBL)** — rather than mis-building. STM32 boards flash over the ST-LINK/SWD probe (not a USB-serial bootloader like ESP32), so the pio upload gate probes the ST-LINK (`_pio_probe_stlink`, reusing `_probe_stlink` + `STM32_Programmer_CLI --list`, falling back to a `pio device list` VID-0483 VCP check) and only WARNS on an inconclusive miss, hard-refusing only a CONFIDENT absence (a bare ST-LINK v2 dongle has no VCP — no false refusals). Config keys: `stm32_backend`, `board`, `framework`, `environment`, `pio_executable`, `pio_core_dir`, `programmer_cli` (PlatformIO) + `auto_bootstrap`, `mcp_repo_url`, `mcp_ref`, `mcp_install_dir`, `auto_update`, `pip_install`, `preflight` (shared / template MCP), `device`. The KV header now carries `backend` / `board` / `port` (registered in `agent_contracts._PARAMETRIZER_OUTPUT_FIELDS['stm32er']`). Emits an `INI_SECTION_STM32ER` block and ALWAYS triggers `target_agents` so a downstream Forker can branch on the result. Global `config.json` defaults `stm32_mcp_server_script` (now `""`), `stm32_mcp_repo_url`, `stm32_mcp_install_dir`, **plus the SHARED `pio_executable` / `pio_core_dir`**, are seeded by `tools._seed_global_agent_defaults`. Visual canvas counterpart of the wrapped `chat_agent_stm32er` Multi-Turn tool.
- **ESP32er** - Bridge to **ESP32 firmware** development via **PlatformIO Core** (`https://platformio.org`). Unlike STM32er — which drives a separate MCP server because STM32CubeIDE has no unified CLI — PlatformIO already ships a complete `pio` CLI, so ESP32er invokes `pio` subcommands **directly** (the Kalier/Executer pattern; no MCP server) over a stdlib-only `agent/agents/esp32er/esp32er.py` (`subprocess` + `urllib` — no `pio` dep in the pool). Runs ONE capability per run selected by `action`: environment/meta (`bootstrap`, `validate`, `system_info`, `boards`); project lifecycle (`create_project`, `write_source`, `read_source`, `list_sources`, `clean`); build & flash (`build`, `upload`, `build_and_upload`, `list_artifacts`, and the one-call lifecycle composite **`scaffold_build_upload`** = create_project→write_source→build→upload→optional-monitor in a SINGLE agent run, fail-safe: it skips only the upload leg with a "built OK" result when no board is connected, and when a board IS present it compiles+flashes in one `pio run -t upload` — this is the fast path that collapses the old 4-separate-call chain and its Multi-Turn round-trips); serial HIL (`device_list`, `monitor` — a bounded `pio device monitor` drained for `monitor_seconds`, and the composite `monitor_session` = upload→monitor); packages & QA (`pkg_install`, `pkg_list`, `pkg_update`, `check`, `test`). **ZERO-CONFIG AUTO-BOOTSTRAP** (`_bootstrap_pio`): with no on-disk `pio_executable` and `auto_bootstrap: true`, ESP32er downloads PlatformIO Core itself — the official `get-platformio.py` installer (with a `pip install platformio` fallback) into `%LOCALAPPDATA%/Tlamatini/platformio` (PLATFORMIO_CORE_DIR) — and validates it, so a user installs **only the board USB driver + Tlamatini**. **SAFETY PREFLIGHT** (`_preflight`, fail-safe): before any build/upload it validates `pio` is resolvable + a `platformio.ini` exists, and for an upload/monitor that a serial port is connected (`_probe_serial` via `pio device list --json-output`; ESP32 flashes over its onboard USB-serial bootloader so NO external JTAG probe is needed for upload — only `debug` would need one), and **REFUSES** rather than run a build/upload that cannot succeed; a non-espressif32 platform is a warning, not a refusal (PlatformIO is multi-target — no shared-linker-script risk like STM32). Config keys: `pio_executable`, `auto_bootstrap`, `pio_install_method`, `pio_core_dir`, `auto_update`, `pip_install`, `preflight`, `board`, `framework`, `environment`, `command_timeout`. Emits an `INI_SECTION_ESP32ER` block (fields `action`, `tool`, `ok`, `returncode`, `success`, `project_dir`, `port`, `environment`, `stage`; body = `pio` output) and ALWAYS triggers `target_agents` so a downstream Forker can branch on `{success}` / `{returncode}`. Global `config.json` defaults `pio_executable`, `pio_core_dir` are seeded by `tools._seed_global_agent_defaults`. NOTE: the FIRST build downloads the espressif32 platform + toolchain (hundreds of MB). **Headless step-debug (`pio debug -x <gdb_script>`) is a planned fast-follow** (needs JTAG: an external probe or the built-in USB-JTAG on ESP32-S3/C3). Visual canvas counterpart of the wrapped `chat_agent_esp32er` Multi-Turn tool.
- **Arduiner** - Bridge to **Arduino firmware** development via the **Arduino CLI** (`https://arduino.github.io/arduino-cli/`). The third microcontroller agent and the direct-CLI sibling of ESP32er: like PlatformIO's `pio` (and unlike STM32er's MCP server), `arduino-cli` is itself a complete CLI, so Arduiner invokes `arduino-cli` subcommands **directly** (the Kalier/Executer/ESP32er pattern; no MCP server) over a stdlib-only `agent/agents/arduiner/arduiner.py` (`subprocess` + `urllib` + `zipfile`/`tarfile` — no extra dep in the pool). **THE MICROCONTROLLER IS SELECTED BY `fqbn`** (Fully Qualified Board Name, e.g. `arduino:avr:uno`, `arduino:avr:mega2560`, `arduino:samd:mkr1000`, `esp32:esp32:esp32`); `port` + `baud` set the upload/monitor link. Runs ONE capability per run selected by `action`: environment/meta (`bootstrap`, `validate`, `system_info`, `boards`, `device_list`); cores & libraries (`core_update_index`, `core_search`, `core_list`, `core_install`, `core_uninstall`, `lib_update_index`, `lib_search`, `lib_list`, `lib_install`); project lifecycle (`create_project`, `write_source`, `read_source`, `list_sources`); build & flash (`build`, `upload`, `build_and_upload`, `clean`, `list_artifacts`); serial HIL (`monitor` — a bounded `arduino-cli monitor --config baudrate=<baud>` drained for `monitor_seconds`, and the composite `monitor_session` = upload→monitor). **ZERO-CONFIG AUTO-BOOTSTRAP** (`_bootstrap_cli`): with no on-disk `arduino_cli_executable` and `auto_bootstrap: true`, Arduiner downloads the arduino-cli binary itself — the platform release archive from `downloads.arduino.cc`, unzipped into `%LOCALAPPDATA%/Tlamatini/arduino-cli` (ARDUINO_DIRECTORIES_DATA/CONFIG point there for read-only-frozen safety) — then runs `config init` + `core update-index`, so a user installs **only the board USB driver + Tlamatini**. (arduino-cli is a Go binary — the bootstrap is a binary download, NOT a pip install like ESP32er.) **AUTO-CORE-INSTALL** (`_ensure_core_installed`): arduino-cli — unlike PlatformIO — does NOT auto-install platforms on compile, so before a build/upload Arduiner derives the FQBN's platform (`packager:arch`) and, when missing and `auto_core_install: true`, runs `core update-index` + `core install` (honoring `additional_urls` for third-party ESP32/STM32/RP2040 cores); when off it REFUSES with the exact `core install` to run. **SAFETY PREFLIGHT** (`_preflight`, fail-safe): before any build/upload it validates `arduino-cli` is resolvable + a sketch (`.ino`) + an FQBN exist, and for an upload/monitor that a serial port is connected (`_probe_serial` via `arduino-cli board list --json`, which also surfaces the matched FQBN); a malformed FQBN is a warning, not a refusal (arduino-cli is multi-vendor — no shared-linker-script risk like STM32). **UNIFORM TEMPLATE PROJECT**: `create_project` scaffolds from the bundled `agent/agents/arduiner/ArduinoTemplateProject/` (the Arduino analog of STM32er's STM32 Template Project and ESP32er's `pio` scaffold) — copies it, renames the `.ino` to the destination folder, and stamps the FQBN/port into the template's `sketch.yaml` profile (the Arduino-native peer of `platformio.ini`). Config keys: `arduino_cli_executable`, `auto_bootstrap`, `arduino_cli_install_dir`, `auto_update`, `preflight`, `fqbn`, `sketch_path`, `auto_core_install`, `additional_urls`, `core_spec`, `lib_spec`, `boards_query`, `warnings`, `build_property`, `extra_compile_args`, `command_timeout`, `port`, `programmer`, `baud`, `monitor_seconds`. Emits an `INI_SECTION_ARDUINER` block (fields `action`, `tool`, `ok`, `returncode`, `success`, `fqbn`, `port`, `sketch_path`, `stage`; body = `arduino-cli` output) and ALWAYS triggers `target_agents` so a downstream Forker can branch on `{success}` / `{returncode}`. Global `config.json` defaults `arduino_cli_executable`, `arduino_cli_install_dir` are seeded by `tools._seed_global_agent_defaults`. NOTE: the FIRST core install + compile downloads the board toolchain so it is slow. Visual canvas counterpart of the wrapped `chat_agent_arduiner` Multi-Turn tool.
- **ESPHomer** - Bridge to **ESPHome** (`https://esphome.io`) — smart-home device firmware from a **simple YAML config (NO C++)** on ESP32 / ESP8266 / RP2040 / BK72xx. The fourth microcontroller agent and a direct-CLI sibling of ESP32er/Arduiner: ESPHome ships a complete `esphome` CLI, so ESPHomer invokes `esphome` subcommands **directly** (no MCP server) over a stdlib-only `agent/agents/esphomer/esphomer.py` (`subprocess` + `glob` — no extra dep in the pool). Runs ONE capability per run selected by `action`: environment/meta (`bootstrap`, `validate`, `version`); device YAML lifecycle (`new_config` — a built-in headless GENERATOR that writes a minimal valid device YAML, the replacement for the interactive `esphome wizard`, from `name`/`platform`/`board`/`wifi_ssid`/`wifi_password`/`led_pin`; `write_config`, `read_config`, `config`, `clean`); build & flash (`compile`, `upload`, `run`, `list_artifacts`, and the one-call lifecycle composite **`scaffold_compile_upload`** = author→config→compile→upload→optional-logs in a SINGLE agent run, fail-safe: it skips only the upload leg with a "compiled OK" result when no board is connected); serial/OTA HIL (`logs` — a bounded `esphome logs` drained for `monitor_seconds`). **ZERO-CONFIG AUTO-BOOTSTRAP** (`_bootstrap_esphome`): with no on-disk `esphome_executable` and `auto_bootstrap: true`, ESPHomer `pip install esphome` into the agent's Python and validates it, so a user installs **only the board USB driver + Tlamatini**. (ESPHome vendors PlatformIO + toolchains at first compile; the bootstrap is a pip install.) **SAFETY PREFLIGHT** (`_preflight`, fail-safe): before any compile/upload it validates `esphome` is resolvable + a device YAML exists, and for an upload/logs/run that a serial port is connected (stdlib `_enumerate_serial_ports` via the Windows SERIALCOMM registry / POSIX `/dev/tty*`) **or** an OTA host is supplied in `port` (first flash USB, OTA after), and **REFUSES** rather than run a build/upload that cannot succeed. Config keys: `esphome_executable`, `auto_bootstrap`, `pip_install`, `auto_update`, `preflight`, `config_path`, `content`, `name`, `platform`, `board`, `led_pin`, `wifi_ssid`, `wifi_password`, `command_timeout`, `port`, `monitor_seconds`. Emits an `INI_SECTION_ESPHOMER` block (fields `action`, `tool`, `ok`, `returncode`, `success`, `config_path`, `name`, `port`, `stage`; body = `esphome` output) and ALWAYS triggers `target_agents` so a downstream Forker can branch on `{success}` / `{returncode}`. Global `config.json` default `esphome_executable` is seeded by `tools._seed_global_agent_defaults`. NOTE: the FIRST compile downloads the platform + toolchain (via PlatformIO under the hood) so it is slow. Visual canvas counterpart of the wrapped `chat_agent_esphomer` Multi-Turn tool.

### Cryptography Agents
- **Kyber-KeyGen** - CRYSTALS-Kyber key pair generation (post-quantum)
- **Kyber-Cipher** - Kyber encryption
- **Kyber-DeCipher** - Kyber decryption

### Utility Agents
- **Parametrizer** - Maps structured output from one agent into another's config.yaml (strict single-lane queue)
- **FlowBacker** - Backs up session logs/configs
- **FlowCreator** - LLM-powered flow designer (system agent, singleton, no canvas connections); reads `agentic_skill.md` and emits a `.flw` JSON. **Also chat-callable (2026-07-22) via the wrapped `chat_agent_flowcreator` tool**: pass `prompt='<objective>'` + `flow_filename='<name>.flw'` and it WRITES a real, canvas-loadable `.flw` to disk (default `<app>/Temp`, or `output_dir=`) instead of rendering onto the canvas — the from-chat "prompt in → .flw out" path. It ships a vendored `result_to_flw.py` (pool agents can't import `agent.*`) to convert `flow_result.json` → `.flw`, and now exits NON-ZERO on failure so a failed generation is reported as FAILURE, not a phantom success (the exit code drives the wrapped-runtime completed/failed verdict; the canvas path is unaffected — it keys off `flow_result.json` + the PID file). Emits `INI_SECTION_FLOWCREATOR` whose header carries `status`, `flw_path`, `flow_filename`, `agent_count`, `connection_count` (registered as Parametrizer source fields in `agent_contracts._PARAMETRIZER_OUTPUT_FIELDS['flowcreator']`)
- **Gatewayer** - HTTP webhook ingress + folder-drop watcher
- **Gateway-Relayer** - Bridges GitHub/GitLab webhooks into Gatewayer
- **Node-Manager** - Infrastructure registry and node supervision

### Terminal/Monitoring Agents (do NOT start downstream)
- **Monitor-Log** - LLM-powered log file monitor
- **Monitor-Netstat** - LLM-powered network port monitor
- **Emailer** - SMTP email on pattern detection
- **RecMailer** - IMAP email receiver/monitor
- **Notifier** - Notification on pattern match (or oneshot from chat). Drops a `notification.json` file that the Tlamatini chat UI polls and renders as an **in-browser DOM popup** (with optional `.wav` sound).
- **Whatsapper** - WhatsApp send/receive; `provider=cloud` (default, official Meta Cloud API, business number) or `provider=web` ("as me", sends from your OWN number via WhatsApp Web, unofficial, one-time QR login)
- **Telegrammer** - Telegram send/receive over official Telegram surfaces; configs keep people as `@username`; `provider=bot` (as the bot) or `provider=user`/"as me" (your own account); `auto` picks per recipient
- **FlowHypervisor** - LLM-powered flow health monitor (system agent)

> **Note**: TeleTlamatini is a long-running active bridge. Whatsapper and Telegrammer are short-lived send/receive workers that start `target_agents` and exit.

---

## FlowCreator AI Skill

The FlowCreator agent uses `agentic_skill.md` to design flows. Key design principles:

1. **Minimize agents** - Fewest agents to accomplish the objective
2. **Sequential chains over parallel fan-out** - Chain agents one-by-one
3. **Starter should be lean** - Only start first agent(s)
4. **Terminal agents at END** - Never start Emailer/Notifier from Starter
5. **Raiser for exceptions** - Don't create Raisers for both sides of binary checks
6. **Parametrizer is a strict single-lane queue** - One source, one target, one-at-a-time

### Common Flow Patterns

```
# Linear chain
Starter -> A -> B -> C -> Ender

# Polling loop with exception
Starter -> A -> Sleeper -> A (loop)
           └-> Raiser (watches for exit condition) -> Alert -> Ender

# Parametrized pipeline
Starter -> Source_Agent -> Parametrizer -> Target_Agent -> Ender

# Fork-join
Starter -> A -> AND_Gate -> C -> Ender
       └-> B ----┘

# Conditional branching
Starter -> A -> Forker -> [path A] B -> Ender
                       -> [path B] C -> Ender

# Clean shutdown with backup
... -> Ender -> FlowBacker -> Cleaner
```

---

## FlowHypervisor Monitoring

The FlowHypervisor (`monitoring-prompt.pmt`) is a watchdog that outputs exactly:
- `OK` - flow is healthy
- `ATTENTION NEEDED { explanation }` - problem detected

Diagnostic checks (in order):
1. User timing constraints vs FLOW ELAPSED TIME
2. Critical errors (FATAL, CRASH, Failed to start agent)
3. Stuck agents (short-lived > 5min with no output)
4. Broken chains (agent finished but downstream never started)
5. Previous alert still valid?

Normal things NOT to flag: FlowHypervisor/FlowCreator activity, Sqler/Mongoxer missing logs, long-running agents running long, "REANIMATED" markers, Parametrizer queue progress messages.
