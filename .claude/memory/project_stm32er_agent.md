---
name: project_stm32er_agent
description: "STM32er (#68) agent bridging the STM32TemplateProjectMCP stdio server on canvas + Multi-Turn"
metadata: 
  node_type: memory
  type: project
  originSessionId: 320d70e0-2710-48c9-b614-f09ac5d57ae0
---
<!--
═══════════════════════════════════════════════════════════════════
  ✦  T L A M A T I N I  ✦   —   "one who knows"
  Created by  Angela López Mendoza   ·   @angelahack1
  Developer · Architect · Creator of Tlamatini
  Tlamatini Author Banner — do not remove (Angela's name is kept in every build)
═══════════════════════════════════════════════════════════════════
-->

2026-05-26: Added **STM32er** (agent #68) on BOTH canvas + Multi-Turn (`chat_agent_stm32er`). It bridges the **STM32 Template Project MCP** (`C:\Development\STM32TemplateProjectMCP`, repo github.com/XAIHT/STM32TemplateProjectMCP) — a FastMCP **stdio** server (`mcp/stm32_mcp_server.py`) exposing **23 tools** that scaffold→author→build→flash→observe STM32F4 firmware via the STM32CubeIDE-bundled toolchain (no IDE GUI).

**Design**: STM32er is a self-contained inline **MCP stdio JSON-RPC client** (`agent/agents/stm32er/stm32er.py`) — spawns the server, does `initialize` handshake + `notifications/initialized`, calls ONE `tools/call` per run, parses CallToolResult (text JSON first, structuredContent fallback), shuts down. Stdlib only (subprocess+json+threading+queue); does NOT import `agent.*` NOR the `mcp` package (only the SERVER needs `mcp`). Mirrors the Kalier/ACPXer self-contained pattern. `action` ∈ the 23 tools (get_config, discover_toolchain_tool, create_project, write_source, read_source, list_sources, clean, build, list_artifacts, flash, build_and_flash, erase, reset, serial_* x5, read_memory, write_memory, live_memory_* x3) **PLUS 2 composites** `serial_session` (connect→send|read→disconnect) and `live_monitor` (start→stream monitor_seconds→read→stop) — needed because serial/live-memory sessions live in-process in the server and die when the per-run server exits. `_tool_ok()` treats no-`ok`-key results (get_config/discover) as success. Emits `INI_SECTION_STM32ER<<<` (KV: action, tool, ok, returncode, success, project_dir, session_id, stage; body=stdout/stderr or JSON). ALWAYS triggers target_agents.

**Embedded client**: global config seeding like Kalier — `tools._seed_global_agent_defaults` injects `stm32_mcp_server_script` / `stm32_mcp_python` / `stm32_template_dir` / `stm32_ide_root` (config.json, Config→URLs / views URL_CONFIG_KEYS) over config.yaml `server_script`/`mcp_python`/`template_dir`/`ide_root` so chat prompts never repeat the path. `mcp_python` is for when the agent's Python lacks `mcp`/`pyserial`.

**Color**: blue/black/white/gray 4-stop "silicon" ramp `#0A0E17→#1E63FF→#8A97A8→#EEF3FB` (white text + text-shadow for legibility over the light end). Distinct from Windower (navy→cyan, no black/gray).

**Reg points (mirrors Kalier exactly)**: migrations 0101_add_stm32er (Agent row, id 84) + 0102_add_chat_agent_stm32er_tool (Tool row); views.py connection view + URL_CONFIG_KEYS; urls.py route; parametrizer SECTION_AGENT_TYPES; agent_contracts _PARAMETRIZER_OUTPUT_FIELDS; agent_paths display_name override ("STM32er"); chat_agent_registry spec (poll 180s, long_running); mcp_agent _EXEC_REPORT_TOOLS; capability_registry signal tokens; CSS (agentic_control_panel + agent_page exec-report caption/cmd/dark-thead); 4 JS files (connectors, canvas-core classMap+3 spots+global, undo 2 spots+global, file-io 1 spot+global); agent_page_chat _mapToolArgsToAgentConfig branch; agentic_skill.md (#67, FlowCreator→#68, count 67→68); prompt.pmt rule 11 routing bullet; monitoring-prompt.pmt (SHORT-LIVED list, STM32ER SPECIAL NOTES, duration, markers, NOT-to-flag).

**Verified**: ruff + py_compile clean on all touched Python; eslint 0 errors (223 pre-existing warnings); migrate applied; 19 tests pass (ExecReportCaptureTests + test_flow_contracts); **end-to-end smoke against the REAL server**: get_config→ok:true (toolchain_ok:true), serial_list_ports found real ST-LINK VCP on COM3 (board is connected!), list_artifacts(empty)→success:false correctly.

**Frozen/source parity (2026-05-26 follow-up)**: verified. build.py bundles it generically — `copytree(agent/agents → install root)` ships the stm32er dir, `agents_descriptions.md` is a REQUIRED copy, `collectstatic` runs pre-bundle (CSS/JS land in staticfiles), config.json shipped. The agent reuses kalier's frozen-safe `get_python_command`/`get_agent_env`/`get_pool_path`; inside a pool subprocess `sys.frozen` is False in BOTH modes so it spawns the MCP server under `sys.executable` (the real Python that runs pool agents) — that Python needs `mcp`/`pyserial`; the `mcp_python` override + clear error cover the gap. Tier-1 orphan reaper covers it (matches `chat_agent_*` prefix). **BUG FOUND+FIXED (affected BOTH modes)**: the wrapped-runtime required-param gate (`tools._ConfigRequirementAnalyzer`/`_find_missing_required_config_paths`) statically scans the agent .py for `if <name> is None`/`if not <name>`/`if <name>==empty` and flags `<name>` as a mandatory config key. My `_as_bool(value,...)` helper had `if value is None:` whose local param `value` collided with the `value:` config field (write_memory) → "missing mandatory non-flow parameters: value" blocked EVERY chat call. Fix: renamed coercion-helper params `value`→`raw` in stm32er.py. **Reusable gotcha**: an agent that reads config via a `_cfg(config,key)` wrapper (hides `config.get` from the analyzer, so keys aren't excluded via keys_with_explicit_default) MUST NOT name any local var in an `if`-guard the same as a config key. Verified after fix: `required_keys ∩ config_keys == []`; wrapped `chat_agent_stm32er` action=get_config → run status completed, ok/success/toolchain_ok true; raw run found real ST-LINK on COM3. Empirically source-mode verified; frozen verified by construction (generic bundling + identical patterns) — a full PyInstaller build is the only remaining 100% check.

**DEFERRED per user ("we'll later work in the documentation")**: README.md (count 67→68, structure, tables, glossary, changelog, API), agents_descriptions.md (sidebar tooltip/canvas Description source — STM32er tooltip is empty-fallback until added), docs/claude/* (agents.md catalog etc.), [[project_doc_refresh_2026_05_09]]-style passes, Tlamatini.md self-knowledge, BookOfTlamatini.md, optional demo-prompt migration. **NOT committed** (per directive + [[feedback_user_owns_git]]). Pattern sibling of [[project_kalier_agent]] / [[project_windower_agent]] / [[project_unrealer_expanded_surface]].
