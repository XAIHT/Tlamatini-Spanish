---
name: project-windower-agent
description: "Windower (#65) Win32 window-manager agent — dual-surface (canvas + chat_agent_windower), ports Windows-MCP window subset"
metadata: 
  node_type: memory
  type: project
  originSessionId: 3da125e7-8396-4839-b5e2-09183567d6e8
---
<!--
═══════════════════════════════════════════════════════════════════
  ✦  T L A M A T I N I  ✦   —   "one who knows"
  Created by  Angela López Mendoza   ·   @angelahack1
  Developer · Architect · Creator of Tlamatini
  Tlamatini Author Banner — do not remove (Angela's name is kept in every build)
═══════════════════════════════════════════════════════════════════
-->

2026-05-21: Added **Windower** agent — the WINDOW MANAGER of the desktop-UI trio (Windower=the window itself, Mouser=clicks-inside, Keyboarder=types-into). Implements the user's "control Windows GUI / Windows-MCP" request as a native, self-contained agent rather than wiring the heavy CursorTouch Windows-MCP server (fastmcp/comtypes/dxcam/uiautomation) — it ports only the **window-management subset** inline with `pywin32` (`win32gui`/`win32con`/`win32process`) + `ctypes`, including the **AttachThreadInput cross-process focus-transfer dance** from Windows-MCP's `bring_window_to_top`.

Ships on BOTH surfaces (same dual pattern as Playwrighter/Unrealer): canvas workflow agent + wrapped Multi-Turn tool `chat_agent_windower`. `action` ∈ list / focus / minimize / maximize / restore / move / resize / move_resize / close / topmost / untopmost / arrange (snap/tile). Matches `window_title` by substring/exact/regex + `match_index`. Emits `INI_SECTION_WINDOWER` (action, window_title, matched, match_count, state, left, top, width, height). State-changing → in Exec Report. ALWAYS triggers `target_agents`; `fail_if_absent=true` hard-exits. Win32 imports guarded → degrades to `state: win32_unavailable` off-Windows.

Counts now: **66 agents / 41 wrapped chat-agents / 73 Multi-Turn tools**. Migrations `0093_add_windower` + `0094_add_chat_agent_windower_tool` (run). FlowCreator entry is #65 in agentic_skill.md (FlowCreator bumped to #66).

Files touched (the full dual-surface recipe): `agent/agents/windower/{windower.py,config.yaml}`; `views.py` (`update_windower_connection_view`) + `urls.py`; migrations 0093/0094; `agentic_control_panel.css` (windower-agent frosted-glass gradient); 4 JS files (`acp-agent-connectors.js`, `acp-canvas-core.js`, `acp-canvas-undo.js`, `acp-file-io.js`) + `eslint.config.mjs` global — connection wiring mirrors Mouser/Shoter exactly (source-side only, 3-param connector, writes target_agents); `chat_agent_registry.py` (ChatWrappedAgentSpec, Mouser-vs-Windower hint split); `mcp_agent.py::_EXEC_REPORT_TOOLS`; `agent_page.css` (exec-report-windower rules); `agent_page_chat.js::_mapToolArgsToAgentConfig` (Flow-Generator branch); `agent_contracts._PARAMETRIZER_OUTPUT_FIELDS['windower']` (auto-merges onto disk-discovered contract, NO _BUILTIN_CONTRACTS entry needed); `parametrizer.SECTION_AGENT_TYPES`; docs (CLAUDE.md, docs/claude/{agents,multi-turn,exec-report,gotchas}.md, README.md, BookOfTlamatini.md, agents_descriptions.md, flowhypervisor monitoring-prompt.pmt).

Verified: ruff clean, eslint 0 errors, 19 tests OK (ExecReportCaptureTests + test_flow_contracts), migrate applied, purpose-map resolves Windower (66 total).

Frozen+source functionality verification (2026-05-21): audited both agents end-to-end. **One real bug found and fixed in Windower**: `win32gui.IsZoomed` does NOT exist in **pywin32 311** (the pinned version) — the original `_window_state` relied on it (with a ctypes fallback that risked 64-bit HWND truncation). Hardened it to use `win32gui.GetWindowPlacement(hwnd)[1] == SW_SHOWMAXIMIZED` (present in ALL pywin32 builds) as the primary maximized check, then a getattr-guarded `IsZoomed`, then an HWND-typed (`argtypes=[wintypes.HWND]`, wrapped) ctypes `user32.IsZoomed` as last resort. The 54 original tests MOCKED win32gui so they never caught the real absence — added 2 regression tests (maximized detected with NO `win32gui.IsZoomed`; HWND-safe ctypes fallback). Verified with a REAL `dispatch(action='list')` against live OS windows: 8 windows enumerated, states correctly classified (4 maximized / 4 normal) via the new GetWindowPlacement path, real work-area + tiling math correct. Now 56 Windower tests. Everything ELSE confirmed functional in both modes (no changes needed): build.py copies `agent/agents` wholesale (both dirs ship next to the exe), installs requirements.txt (pywin32) AND runs `playwright install` into the PYTHON_HOME python that pool agents use in frozen mode, runs `migrate` (seeds 0093/0094/0095 rows); `agent_paths.get_agents_root()` frozen→`dirname(sys.executable)/agents` matches the copy target; Playwright import + chromium binary present in source. Pool agents degrade gracefully if a dep is missing (Windower→`state: win32_unavailable`; Playwrighter→`status: error`). **Takeaway for future win32 pool agents: prefer `GetWindowPlacement` over `IsZoomed`, and HWND-type any ctypes user32 call.**

Doc pass (2026-05-21): completed a deep cross-doc sweep so BOTH Windower and Playwrighter are fully + non-duplicatively documented. Fixed gaps found: KIMI.md (5 stale "65"→66 counts + added the Windower catalog entry; Playwrighter was already present), docs/claude/INDEX.md (65→66 + Windower in parenthetical), CLAUDE.md (2 more stale "65" tree-comment counts at lines 42/115 that the earlier pass missed + added playwrighter/ to the tree for parity with windower/), agents_descriptions.md ("65 agent types"→66 header sentence the row-add had missed), BookOfTlamatini.md (added a Playwrighter glossary entry for parity with Windower's; fixed the Parametrizer "source agents" list from a stale 18 to the correct 24 incl. Playwrighter/Shoter/Mouser/Unrealer/Reviewer/Analyzer; added a new top changelog entry covering the 0095 demo prompts + the two 54-test suites), README.md (added two new chat tutorials §3.12 command-a-window + §3.13 drive-a-browser, with matching ToC anchors, placed at the end of ch.3 so no renumber). Verified zero remaining "65" agent-counts in any *.md. Not-relevant files left untouched: ACPX.md, VERSIONING.md, NEW_AGENT_RECOMMENDATIONS.md, create_new_agent/mcp.md, SKILL.md, docs/claude/{architecture,frontend,mcp-tools,acpx}.md.

Demo prompts (2026-05-21): migration `0095_add_windower_playwrighter_demo_prompts.py` APPENDS 4 Catalog-of-Prompts demos at the tail (slots 51-54, no renumber — catalog stays contiguous 1-54): 51 WINDOW SPOTLIGHT (Windower basic: maximize+list+close Notepad), 52 WINDOW CHOREOGRAPHY (Windower medium: restore→arrange left/right/top-left→move_resize→list→close), 53 BROWSER SPOTLIGHT (Playwrighter basic: example.com extract+assert+screenshot), 54 BROWSER WIZARD (Playwrighter medium: Wikipedia search fill→click→wait→extract→assert→screenshot). Both Playwrighter demos use `headless=false` so the browser is visible. Preconditions: **Multi-Turn ONLY** (NOT ACPX — these are wrapped chat_agent_* tools, not behind the ACPX/Skill surface, unlike the 0090 skill demos). Forward+reverse migrate round-trips clean; 0002 docstring map updated. Catalog contiguity contract (dropdown breaks at first gap) preserved by appending — see [[project_reviewer_analyzer_demo_prompts]].

Test coverage (2026-05-21): `agent/test_windower_agent.py` — **54 tests** following the `test_de_compresser.py` pattern (importlib `spec_from_file_location` loader with cwd save/restore + root-handler cleanup; win32 NEVER touched — `enum_windows`/`win32gui` mocked). Classes: MatchWindows, ComputeArrangeRect, WindowState, BringToFront (the AttachThreadInput dance), Dispatch (all 12 actions + edge cases), EmitSection, MainEndStage (target_agents always start + 1 section + fail_if_absent exit code), RegistryIntegration, MigrationPresence. Playwrighter got the parallel `test_playwrighter_agent.py` (54 tests, fake `playwright.sync_api` via sys.modules patch.dict — no real browser). Both ruff-clean; 162 tests green when run with the related suites. Related: [[project_playwrighter_agent]], [[project_acpxer_added]], [[feedback_update_agent_docs]]. Frozen build needs no change (build.py copies agent/agents wholesale; pool agents run on the user's Python where pywin32 is pip-installed).
